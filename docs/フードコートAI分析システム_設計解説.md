# フードコート AI 分析システム 設計解説

> 2026-09-21: Q&Aは期間→手法→必要な売価/原価確認。選んだ手法の知識パックだけを分析前に読む（[management-warehouse](./management-warehouse/README.md)）。KGI/KPI/KFIは改善管理時のみ。共通規則は `_shared/business_goal_metrics.ts`。日次/期間キャッシュは `foodcourt-analysis-ai-v27-capacity-cap`。

> 2026-09-10 更新: 反証Gemini・評価Groq・画像Geminiが標準。設定・障害対策は [現行のモデル構成](./FOODCOURT-AI-RELIABILITY.md) を優先。以下に残るClaude構成・旧費用・旧実測は変更前の履歴であり、現在値ではない。

> 数値予測の更新: 2026-09-10、本番反映済み。現行の詳細正本は [FOODCOURT-FORECAST-AUDIT.md](./FOODCOURT-FORECAST-AUDIT.md)。AI回答・RAGの品質設定とは別の評価系統です。数値予測以外のモデル名・運用設定・件数は各節の確認日時点の記録で、今回の再監査対象ではありません。

MARUGO S（東京ドーム内フードホール「FOOD STADIUM TOKYO」）の売上・客数データを解析する
**5エージェント構成のマルチAIパイプライン**の設計と動作を解説する。

現行キャッシュ: `foodcourt-analysis-ai-v27-capacity-cap`（Q&A・日次・期間）。旧表記 `v16-loop-learning` は履歴。
関連ファイル: [`supabase/functions/_shared/foodcourt_compare.ts`](../supabase/functions/_shared/foodcourt_compare.ts)  
関連ドキュメント: [フードコート学習システム構造.md](フードコート学習システム構造.md)、[フードコートAIループシステム全体解説.md](フードコートAIループシステム全体解説.md)、[フードコート売上分析_設計書.md](フードコート売上分析_設計書.md)、[フードコート日報システム.md](フードコート日報システム.md)

---

## 1. システム全体の設計思想

### 1-1. 解決した3つの問題

| 問題 | 原因 | 解決策 |
|---|---|---|
| 「毎回同じことしか言わない」 | 1つのAIが全てを担当 → 平均的な言い回しになる | 役割分担（専門AI×3→反証AI→統合AI） |
| 「数字を言い換えるだけで中身がない」 | AIが数字を生成するため、正確さより流暢さを優先 | **コードが全ての数値を事前計算**、AIには解釈だけさせる |
| 「予測モデルとAI解説が矛盾する」 | 予測値と解釈の区別が不明瞭だった | 最新予測・評価を共有し、解釈用乗算係数を採用方式の因果説明と混同しない |

### 1-2. 基本原則

- **AIは数字を作らない**: 売上・客数・順位・比率はすべてコード（TypeScript）が計算し、AIにテキストで渡す
- **役割分担で深度を上げる**: 各専門AIが担当領域のみを深掘りし、統合AIが矛盾を解消する
- **反証ファースト**: 専門AIの主張を別AIが批判的にレビューしてから統合する（品質ゲート）
- **異なる知識・異なる思考回路を持つAIを意図的に組み合わせる**: 同じAIを複数役に使うと「癖」が共通し盲点も共通する。各役割に最適なプロバイダーを選ぶことで視点の多様性を確保する

---

## 2. 3専門AIの「知識の違い」が分析の核心

最も重要な設計判断は、**専門AI①②③をすべて異なるプロバイダー・モデルで構成している**ことである。

### なぜ同じAIを複数役に使ってはいけないか

同じモデルに「数値分析」と「運営改善」を別々に依頼しても、同じ学習データ・同じ思考の癖から生まれた回答が出てくる。専門性を偽装しているだけで、実態は1つのAIが2役を演じているに過ぎない。

### 3つの専門AIが持つ「異なる知識の種類」

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    3専門AIの知識領域の違い                               │
├─────────────────┬───────────────────┬──────────────────────────────────┤
│ 専門AI①         │ 専門AI②           │ 専門AI③                         │
│ Groq             │ Gemini            │ Grok (xAI)                       │
│ gpt-oss-120b     │ gemini-3.5-flash  │ grok-3-mini                      │
├─────────────────┼───────────────────┼──────────────────────────────────┤
│ 【強みの知識】   │ 【強みの知識】     │ 【強みの知識】                   │
│                  │                   │                                  │
│ 数値・統計的     │ 世界の出来事・      │ 現場で明日打てる打ち手の具体化    │
│ パターン分析     │ エンタメ・人物      │ （Chat Completions。X検索なし）  │
│                  │ に関する広範な      │                                  │
│ 「この数字は     │ 知識               │ ※最新のX話題は別経路の            │
│ 統計的に         │                   │  Xトレンドブリーフ（x_search）    │
│ 何を意味するか」 │ 「NiziUとは        │  が担当する                       │
│ を推論           │ どんな客層が       │                                  │
│                  │ 来るアーティスト   │                                  │
│ ← 数値分析・    │ か」を知っている   │ ← 運営改善（打ち手提案）に最適   │
│ 他店舗比較に最適 │                   │                                  │
│                  │ ← イベント・天気   │                                  │
│                  │ 分析に最適         │                                  │
└─────────────────┴───────────────────┴──────────────────────────────────┘
```

### 具体的な分析例：同じ「野球の日」をどう読むか

**専門AI①（Groq）の視点 — データから読む:**
> 「プロ野球（ドーム本体）の日の学習係数は×1.89（n=6）で再現性あり。今日の実績は係数予測を7%下回っており、天気（雨）の係数×0.88が影響した可能性がある」

**専門AI②（Gemini）の視点 — イベント知識から読む:**
> 「巨人vs阪神戦は関西からの遠征ファンが多く、試合後の時間帯に小腹を満たしたい需要が特徴的。デーゲームのため13時〜15時のピーク後に夕方の谷間が生じる傾向がある」

**専門AI③（Grok）の視点 — 現在のトレンドから読む:**
> 「X上では最近の阪神ファンに『球場グルメを楽しむ』投稿が急増中。ビールよりも写真映えするサイドメニューへの関心が高い。試合終盤〜終演後30分が最大の商機で、スピード提供と見映えの両立が打ち手の軸になる」

→ **3つの視点が合わさって初めて「今日・この試合・この客層に何をすべきか」という深い分析が成立する。**

---

## 3. 5エージェント構成

### 3-1. パイプライン全体図

```
[コードが事前計算した分析コンテキスト]
  ├─ 対象日/期間の事実（順位・シェア・自店史比較・要因分解）
  ├─ 競合プロファイル（11店の業態・価格帯・飲食傾向）
  ├─ イベント相関（東京ドーム3会場×客数の実績）
  ├─ 天気相関（雨の日vs晴れの日の客数差）
  ├─ 店舗間相関（ピアソン相関・カニバリ/アンカー候補）
  ├─ 異常値（Zスコア≥2の突出日/落込日）
  ├─ 学習型モデルの予測（今後14日・自己採点付き）
  └─ 予測値と参考の解釈用統計（曜日/イベント/天気の係数・参考誤差）
         │
         ├───────────────────────┬──────────────────────────┐
         ▼                       ▼                          ▼
  [専門AI①]               [専門AI②]                [専門AI③]
  数値分析・他店舗比較      イベント・天気              運営改善
  Groq                     Gemini                     Grok (xAI)
  openai/gpt-oss-120b      gemini-3.5-flash           grok-3-mini
  統計的パターン知識         エンタメ・世界知識          現場打ち手の具体化
  最大700トークン出力        最大700トークン出力         最大700トークン出力
         │                       │                          │
         │                       │                   (+ 並列) Xトレンドブリーフ
         │                       │                   grok-4.5 + x_search
         └──────────┬────────────┘                          │
                     ▼                                       │
           [反証AI④]                                        │
           品質管理・反証                                     │
           Gemini (gemini-3.5-flash)                        │
           批判的思考・論理検証に特化                         │
           最大650トークン出力                                │
                     │                                       │
                     └──────────────┬────────────────────────┘
                                    ▼
                          [統合AI⑤]
                          最終回答生成
                          OpenAI (GPT-5.6 Luna)
                          推論モデル・矛盾解消に特化
                          最大1400〜1800トークン出力
                                    │
                                    ▼
                          [最終出力]
                          7見出し構造の分析サマリー
                          または Q&Aへの回答
```

### 3-2. 並列・直列の実行順序

```
ラウンド1（並列・同時実行）: 専門AI①②③ → 約2〜4秒
      ↓
ラウンド2（直列）: 反証AI④ → 専門AI①②③の出力を受け取り批判的レビュー → 約1〜2秒
      ↓
ラウンド3（直列）: 統合AI⑤ → 全ての情報を受け取り最終出力 → 約3〜6秒
```

### 3-3. 品質評価ループ＝第6のエージェント（2026-07-07追加・2026-07-21 Q&A段階導入）

統合AI⑤の最終出力の後段に、**評価AI⑥**によるループを追加できる（AIループエンジニアリング）。

```
統合AI⑤の回答 → [評価AI⑥] 5軸採点（正確性/論理性/専門性/実用性/根拠・JSONのみ）
   → 合格（現在は総合70＋各項目65＝総合−5点の許容幅）なら返却
   → 不合格なら「改善点だけ」を統合AI⑤に渡して再生成 → 再評価
   → 上限（各surfaceとも最大2回）に達したら最高得点の回答を返す
```

- 再ループ時に専門AI①②③・反証AI④は**再実行しない**（メモを使い回し、統合AI⑤だけ再生成）＝コスト増を最小化
- 合否は評価AIの自己申告ではなくコード側が閾値判定。全ループは `foodcourt_ai_loop_runs` / `foodcourt_ai_loop_iterations` に保存
- **未設定時はOFF**。本番は `ask` のみ明示ON、`daily_summary` / `period_summary` / `weekly_report` は明示OFFで段階導入中
- 詳細な仕組み・有効化手順は [フードコートAIループシステム全体解説.md](フードコートAIループシステム全体解説.md)、設計・実装ログは [AI_LOOP_ENGINEERING_DESIGN.md](AI_LOOP_ENGINEERING_DESIGN.md) を参照

### 3-4. 現場日報データの Q&A 組み込み（2026-07-08追加）

#### 追加の背景

数値データだけでは「なぜ売上が動いたか」の因果を説明しきれない。現場スタッフが日報に記録した**施策・動員数・担当者評価・課題**をAIの分析材料に加えることで、施策効果の仮説検証が可能になる。

#### データフロー

```
[日報ページ] 施策・動員数を入力・保存
  ↓ (PUT /foodcourt/daily-logs)
  foodcourt_daily_logs テーブル

[Q&Aリクエスト]
  /foodcourt/ask ハンドラ
    ↓ 直近60日分を取得
  buildDailyLogsContext(dailyLogs)
    ↓ 日付ごとのテキストブロックに変換
  answerFoodCourtQuestion(... dailyLogs)
    ↓ opsUser プロンプト + contextBlock に注入
  AI 回答（施策と実績を照合して出力）
```

#### AI への注入内容

| 注入先 | 内容 |
|---|---|
| 運営改善専門AI (Grok) の `opsUser` | `# 現場日報・施策記録` ブロック（施策・評価・課題） |
| 統合AI の `contextBlock` | 同上（全エージェントが参照できるよう最終コンテキストにも含める） |
| 統合AIのシステム指示 **(12)** | 「施策を打った日/翌日の実績と照合せよ。担当者評価は主観なのでデータで裏取りし、整合・不整合を明示せよ」 |

#### Q&A の変化

| 以前 | 追加後 |
|---|---|
| 「7/5は客数が増えました」 | 「7/5は試食配布（14〜15時）を実施。担当者は+10組と評価しており、実績でも14〜16時の客数が前日比+18%と一致。ただしナイター客（42,000人規模）の影響と切り分けるには…」 |
| 「施策の効果は不明」 | 「7/12は声かけ強化を実施したが客数は前日比-3%。同日の動員数は30,000人でやや少なく、施策効果よりイベント規模の影響が大きい可能性」 |

詳細は [フードコート日報システム.md](フードコート日報システム.md) を参照。

---

## 4. 各AIエージェントの詳細

### 専門AI① ── 数値分析・他店舗比較専門（Groq）

| 項目 | 内容 |
|---|---|
| プロバイダー | Groq |
| モデル | `openai/gpt-oss-120b`（`FOODCOURT_GROQ_MODEL`環境変数で変更可。未設定時も同モデル） |
| フォールバック | Gemini（`buildFoodCourtProviderOrder('groq')`） |
| 最大出力 | 600〜700トークン |
| 出力用途 | 統合AIへの「分析メモ（下書き）」 |

**Groq / GPT-OSS を使う理由:**
- GPT-OSS 120B は数値・統計の推論と長い入力コンテキストに適する本番安定枠
- Groqの専用ハードウェア（LPU）による超高速推論（他の2専門AIと並列実行するため速度が重要）
- **「数字と数字の関係性から意味を引き出す」という分析的思考が強み**
- 失敗時は Gemini へ退避し、専門AI①だけが全滅しないようにする

**担当する分析タスク:**
1. 客単価・順位が業態（競合プロファイル）から見て妥当か想定外かを判定
2. 真の競合（同じ来店動機・価格帯で客を奪い合う相手）を特定
3. 売上増減が「客数要因」か「客単価要因」かを要因分解データで切り分ける
4. 店舗間相関（カニバリ/アンカー）を業態文脈で解釈
5. 異常値（Zスコア）の突出日/落込日を平常と切り離して注記
6. 予測の評価対象・母数・参考集計の限界を踏まえた所見

---

### 専門AI② ── イベント・天気需要ドライバー専門（Gemini）

| 項目 | 内容 |
|---|---|
| プロバイダー | Google |
| モデル | `gemini-3.5-flash`（`FOODCOURT_GEMINI_MODEL`環境変数で制御） |
| フォールバック | Groq `openai/gpt-oss-120b` |
| 最大出力 | 600〜700トークン |
| 出力用途 | 統合AIへの「分析メモ（下書き）」 |

**Geminiを使う理由:**
- Googleの膨大なウェブコーパスから学習しており、**アーティスト・スポーツチーム・興行イベントに関する世界知識が非常に豊富**
- 「NiziUライブ＝若年女性中心で物販後の軽い飲食需要」「阪神ファンは関西から遠征が多い」といった、イベント×来店客層の推論に強い
- 天気・季節・地理的文脈も組み合わせた複合的な分析が得意

**担当する分析タスク:**
1. イベント種別・規模・客層から、客数・売上への影響メカニズムを説明
2. 野球（対戦相手/デーナイター）、ライブ（アーティスト/客層）、小ホール（後楽園・カナデビア）の差異を考慮
3. 天気（雨・猛暑等）とイベント有無の交互作用
4. 今後の予定イベントと想定される影響・打ち手

---

### 専門AI③ ── 運営改善専門（Grok / xAI）

| 項目 | 内容 |
|---|---|
| プロバイダー | xAI（Elon Musk率いるAI企業） |
| モデル | `grok-3-mini`（`FOODCOURT_GROK_MODEL`環境変数で変更可） |
| APIキー変数 | `XAI_API_KEY` |
| フォールバック | Groq `openai/gpt-oss-120b` |
| 最大出力 | 600〜700トークン |
| 出力用途 | 統合AIへの「運営改善メモ（下書き）」 |

**Grokを使う理由と、その正確な範囲（重要）:**

> **注意（2026-08 修正）**: 以前ここには「Grok 3 mini は X のリアルタイム投稿データを学習しているので、現在進行形の流行を知っている」と書かれていたが、これは誤解を招く記述だった。専門AI③ は Chat Completions (`/v1/chat/completions`) 経由で呼ばれており、この経路は関数ツールしか受け付けず **`x_search` が使えない＝X を一切検索していない**。学習データにはカットオフがあるため、この経路だけでは「今週の話題」は原理的に取得できない。

現在は役割を2つに分離している:

| 役割 | 経路 | 実際にやっていること |
|---|---|---|
| 専門AI③（運営改善メモ） | Chat Completions + `grok-3-mini` | 学習済み知識＋渡されたデータで打ち手を書く（X検索なし） |
| Xトレンドブリーフ | **Responses API + `x_search`** + `grok-4.5` | 指定期間の X を実際に検索し、引用URL付きで返す |

X の最新トレンドは後者が担当し、その結果を統合AIの材料として渡す。検索が実行された証跡（`x_search_call` / X ドメインの引用URL）が取れない応答は破棄されるため、「検索したふり」の出力が混入しない。

| Xトレンドブリーフが取ってくるもの | 運営改善への活用 |
|---|---|
| 今話題のアーティストのファン層の反応 | 「このライブの客はどんな食を好むか」の推論 |
| スポーツチームのファンの当日の話題 | 「試合後の行動パターン・消費傾向」の予測 |
| 直近の食トレンド・SNS映えの傾向 | 「今どんな見せ方をすれば刺さるか」の提案 |
| 話題になっているキャンペーン・演出事例 | 現場で明日から使えるアイデアの引き出し |

いずれも**外部知見であり売上の根拠には使わない**。統合AIには出所を明示して書くよう指示している。

**MARUGO S固有データとの組み合わせが強い:**
コードから渡す「学習係数・予測・競合データ」がGrokをMAROUGO Sの現実に接地させる一方、Grokのトレンド知識が「その数字の背景にある客の心理・行動」を補完する。両者は競合しない。

**担当する分析タスク:**
1. 明日から現場で試せる打ち手のみを出力（数値分析・イベント分析の断定は行わない）
2. 仕込み量・人員配置・ピーク対応・声かけ・セット提案・商品の見せ方

**厳守ルール:**
- データにない販売点数・原価・スタッフ人数は作らない
- 打ち手には必ず「狙う客層/来店動機」「実施条件」「見るべきKPI」をセットで付ける

---

### 反証AI④ ── 品質管理・反証専門（Gemini）

| 項目 | 内容 |
|---|---|
| 全 surface | Gemini gemini-3.5-flash → Groq |
| モデル設定 | `FOODCOURT_GEMINI_MODEL`（既定 `gemini-3.5-flash`） |
| APIキー変数 | `GEMINI_API_KEY` / `GOOGLE_API_KEY` / `VISION_API_KEY` |
| 最大出力 | 550〜650トークン（日次は timeout 15秒、他 surface は 25秒） |
| 出力用途 | 統合AIへの「反証メモ」 |

**Geminiを反証に使う理由:**
- ClaudeのHTTP 400が繰り返されたため、本番で成功実績のあるGeminiを標準にした。言い過ぎ・因果断定・日付ずれの検査を継続する。
- 日次は定型の単日サマリーのため timeout を短めに、Q&A・期間・週次はコンテキストが厚いため timeout を長めにする。
- 評価AI⑥は別モデルのGroq。共通の5軸基準とコードによる数値監査は維持する。
- 反証メモは統合AIへの編集指示であり、最終回答そのものではない。

**担当する分析タスク:**
専門AI①②③のメモに含まれる以下の問題を検出する:
- 言い過ぎ（弱いデータからの過剰な断定）
- 根拠不足（データなしの主張）
- 相関と因果の混同（「Aの日は売上が高いから→Aが原因」）
- 対象日/期間の取り違え
- データにない数字の混入（捏造）

**出力形式:**
```
採用してよい主張: 〜〜（根拠が明確なもの）
弱めるべき主張: 〜〜（断定→仮説に変えるべきもの）
禁止すべき断定: 〜〜（データに根拠がないもの）
```

---

### 統合AI⑤ ── 最終回答生成（OpenAI）

| 項目 | 内容 |
|---|---|
| プロバイダー | OpenAI |
| モデル | `gpt-5.6-luna`（`FOODCOURT_OPENAI_MODEL`環境変数で制御） |
| APIエンドポイント | `/v1/chat/completions`（Chat Completions API） |
| フォールバック順 | OpenAI失敗 → Gemini → Groq |
| 最大出力 | 1400〜1800トークン |
| 出力用途 | **ユーザーへの最終回答** |

**GPT-5.6 Lunaを使う理由:**
- 複数の専門AIメモと反証メモを統合する推論・編集タスクに対応
- 反証AI④が禁止した断定を「仮説」「データ不足」へ言い換える編集判断を担う
- 公式単価が入力$1・出力$6/1Mトークンで、従来のo4-miniとほぼ同等コスト
- 品質ループの初回生成と改善再生成で同じモデルを使い、モデル切替による品質低下を防ぐ

**担当するタスク:**
1. 専門AI①②③のメモと反証AI④の指摘を受け取り、矛盾を解消
2. 反証メモで「禁止された断定」を使わず「仮説」「データ不足」と言い換える
3. 7見出し構造（日次・期間サマリー）またはQ&A回答として整形

---

## 5. プロバイダー一覧と選定理由まとめ

| エージェント | プロバイダー | モデル | 選定の核心理由 |
|---|---|---|---|
| 専門AI① 数値分析 | Groq | openai/gpt-oss-120b | 数値統計パターン推論に適合。高速推論。失敗時は Gemini へ |
| 専門AI② イベント・天気 | Google | gemini-3.5-flash | アーティスト・スポーツ等の世界知識が最も豊富 |
| **専門AI③ 運営改善** | **xAI** | **grok-3-mini** | 現場で明日打てる施策の具体化。Chat Completions 経由のため **X検索は行わない**（学習済み知識のみ） |
| **③' Xトレンドブリーフ** | **xAI** | **grok-4.5** (`x_search`) | **Responses API で X を実際に検索**。指定期間の話題を引用URL付きで取得し統合AIへ渡す。検索証跡が無い応答は破棄 |
| 反証AI④ 品質管理 | Google | gemini-3.5-flash | 成功実績のある経路で矛盾・根拠不足を監査 |
| 統合AI⑤ 最終生成 | OpenAI | gpt-5.6-luna | 複数AIメモの矛盾解消・統合整形を担当 |
| 評価AI⑥ 品質採点 | Groq | openai/gpt-oss-120b | 5軸JSON採点。合否はコード側で再判定 |

---

## 6. 3つの分析モード

### 6-1. 日次サマリー（`generateFoodCourtDailySummary`）

特定の1日のレポートを対象とした自動生成サマリー。

**出力フォーマット（固定7見出し）:**
```
【総評】 対象日の総合評価（強い/弱い/平常）+ 根拠
【売上】 売上・FC平均比・順位 + 意味づけ
【客数】 客数・FC平均比・順位 + 意味づけ
【客単価】 客単価・FC平均比 + 業態文脈での意味づけ
【競合環境】 自店の業態・真の競合・強みの説明
【この日の評価（条件別）】 自店史比・曜日比・イベント/天気の影響・要因分解
【直近の勢い】 直近推移・前回分析の自己検証・次のKPIまたは打ち手
```

**前回分析の自己検証機能:**
前回（直近1営業日）の分析テキストを`priorSummary`として統合AIに渡し、
「前回の見立てが今回の実績でも裏付けられたか、変わったかを一言検証する」よう指示する。
→ 毎日同じ言い回しにならず、連続性のある分析になる。

---

### 6-2. 期間サマリー（`generateFoodCourtPeriodSummary`）

「期間で見る」モードで任意の開始日〜終了日を選んだときに生成。

- 期間内の曜日・イベント・天気の**構成比**を踏まえた分析
- キャッシュキー: `(store_key, start_date, end_date)`

---

### 6-3. Q&A（`answerFoodCourtQuestion`）

ユーザーの自然文の質問に回答するチャット形式。

**時間軸の正確性（重要）:**
画面に表示中のレポートの対象日を`viewingDate`として全エージェントのプロンプトに注入する。
この仕組みがないと、AIは「どの日の話か」をイベントの派手さだけで判断し、時間軸がずれた回答になる。

**会話継続:** 直前8メッセージの履歴を統合AIに渡し、「その店は？」「もっと詳しく」等の指示語が機能する。

---

## 7. 数値予測・評価とAI解説の接続

### 7-1. 予測の正本と解釈を分ける

2026-09-10から、安全補正乗算（`mult-factor-v4-safe`）、直近4回同曜日（`weekday-4-v1`）、類似日（`similar-days-v1`）、客数・売上別の正則化回帰（`ridge-direct-v1`）、3候補の等重み平均（`ensemble-v1`）の5方式を毎朝05:00 JSTに発行する。予測値とその時点の入力を台帳に保存し、後から書き換えない。

| 用途 | オブジェクト | 読み方 |
|---|---|---|
| 事前予測と発行情報 | `foodcourt_forecast_issuances` / `foodcourt_forecast_snapshots` | 後日の実績で採点する正本 |
| 最新表示用予測 | `forecast_predictions` | 画面・AI向けキャッシュ。精度の正本ではない |
| 解釈用係数・統計 | `foodcourt_forecast_factors` | 乗算係数は採用方式の因果説明と同一ではない |
| 参考の再計算履歴 | `foodcourt_forecast_history` | 本番精度・蓄積による改善の証明には使わない |

### 7-2. 統計拡張（stats-ext-v1）

係数の信頼区間、条件別のサンプル数、交互作用、残差の偏り、効果量はAIの解釈を補助する。これらを算出しただけで採用モデルの予測値が補正されるわけではなく、因果も確定しない。

### 7-3. 学習進化画面の読み方

`foodcourt-evolution.html` の概要は「本番・事前予測」と「参考・過去の再計算」を切り替えて表示する。主評価は直近28日・1日前。14/28/56日、0/1/7/14日前、5方式の比較・将来予測を確認できる。

- WAPE、MAE、偏り、同曜日基準比と各母数を確認する。MAPEは実績が正の日の補助指標。
- 割合は比率×100で表示する。値が1.5を超えても割合の単位を変更しない。
- 初期の本番評価は0日・蓄積中が正常。夜間（JST09:00以降）の発行は朝の採点対象から外す。
- 学習曲線・全記録は参考再計算。方式・評価バージョンをまたぐ差分は比較不可とする。
- 予測幅は同方式・同先行日数の確定誤差20日以上を要する経験的80%幅。本番不足時は参考幅と明示し、それも不足なら非表示。的中確率の保証ではない。

主評価は朝の1日前予測・直近28暦日。月曜日に、現行・候補・同曜日基準の共通21日以上で客数と売上のMAEがともに現行より5%を超えて改善し、前後半（各7日以上）でも両方改善することを確認する。同曜日基準以外の候補は同曜日基準にも両方勝つ必要がある。採用後28日は再切替を待つ。条件未達なら現行方式を維持する。

APIは既存の認証・店舗スコープを維持して `/foodcourt/evolution-history` の `forecast_audit` を返す。ブラウザから台帳へ直接アクセスしない。実装・検証の正本は [FOODCOURT-FORECAST-AUDIT.md](./FOODCOURT-FORECAST-AUDIT.md)。

## 8. コードによる事前計算（AIに数字を作らせない）

| 計算関数 | 計算内容 |
|---|---|
| `buildTargetDayFacts()` | 対象日の順位・シェア・自店史比・同曜日比・イベント/天気・要因分解・直近推移 |
| `buildPeriodFacts()` | 期間合算の順位・シェア・自店史比・イベント/天気の構成比 |
| `buildBaseInsights()` | 全期間の傾向サマリー（最高/最低日・曜日差・前半→後半トレンド等） |
| `buildEventCorrelation()` | イベント有無・カテゴリ別の平均客数・売上差（サンプル数付き） |
| `buildWeatherCorrelation()` | 雨/晴れ別・気温帯別の平均客数・売上差 |
| `buildCompetitorContext()` | 競合11店のプロファイルテキスト |
| `buildContributionDecomposition()` | 売上=客数×客単価の前半→後半変化の3要因分解 |
| `buildStoreCorrelation()` | 各店とのピアソン相関係数（カニバリ/アンカー候補） |
| `buildAnomalyDays()` | Zスコア≥2の突出日/落込日リスト（イベント・天気付き） |
| `buildForecastContext()` | 学習型モデルの予測値・バックテスト自己採点 |
| `buildForecastFactorsContext()` | DB保存済みの学習係数（曜日/イベント/天気）テキスト化 |

---

## 9. プロバイダー設定と環境変数

### 環境変数一覧（Supabase Secrets）

| 変数名 | 用途 | 現在の設定 |
|---|---|---|
| `GROQ_API_KEY` | Groq API認証（専門AI①） | 設定済み |
| `FOODCOURT_GROQ_MODEL` | 専門AI①のモデル名（フードコート専用） | 未設定（デフォルト: `openai/gpt-oss-120b`） |
| `GEMINI_API_KEY` | Gemini API認証（専門AI②） | 設定済み（有料API想定） |
| `FOODCOURT_GEMINI_MODEL` | 専門AI②のモデル名 | `gemini-3.5-flash` |
| `XAI_API_KEY` | xAI API認証（専門AI③・Xトレンド） | **設定済み** |
| `FOODCOURT_GROK_MODEL` | 専門AI③のモデル名 | 未設定（デフォルト: `grok-3-mini`） |
| `FOODCOURT_X_SEARCH_ENABLED` | Xトレンドブリーフの有効/無効 | 未設定（デフォルト: **有効**）。`false` で停止 |
| `FOODCOURT_X_SEARCH_SOFT_TIMEOUT_MS` | ブリーフを待つ上限。超えたら無しで分析続行 | 未設定（デフォルト: `18000`、上限60000） |
| `FOODCOURT_X_SEARCH_CACHE_TTL_MS` | 成功ブリーフのキャッシュ保持時間（同日の再検索を防ぐ） | 未設定（デフォルト: `21600000`＝6時間） |
| `JOURNAL_GROK_X_SEARCH_MODEL` | `x_search` に使うモデル（journal と共用） | 未設定（デフォルト: `grok-4.5`） |
| `GROK_X_SEARCH_LOOKBACK_DAYS` | X を遡って検索する日数（journal と共用） | 未設定 |
| `GROK_X_SEARCH_MAX_TOOL_CALLS` | 1回のブリーフで許す検索回数（journal と共用） | 未設定（デフォルト: `4`、上限8） |
| `GROK_X_SEARCH_MAX_OUTPUT_TOKENS` | ブリーフの出力上限。grok-4.5 は reasoning も算入されるため小さすぎると本文が出ない | 未設定（デフォルト: `3000`、下限600/上限8000） |
| `FOODCOURT_X_SEARCH_TOPIC` | **Xで何を検索するか**。デプロイせずに調整できる | 未設定（デフォルト: 下記の食トレンド4観点） |
| `FOODCOURT_X_SEARCH_ASK_INTENT_GATE` | Q&Aで、トレンドと無関係な質問はX検索を省く | 未設定（デフォルト: **有効**） |
| `FOODCOURT_CRITIC_PROVIDER` | 反証AIの任意設定 | 既定 `gemini` |
| `FOODCOURT_EVALUATOR_PROVIDER` | 評価AIの任意設定 | 既定 `groq`。旧LOOP設定は使わない |
| `FOODCOURT_TENANT_GEMINI_MODEL` | テナント画像抽出専用 | 既定 `gemini-3.5-flash` |
| `FOODCOURT_CLAUDE_MODEL` | 明示的な復旧・比較時のみ使用 | 既定 `claude-haiku-4-5`。標準経路では呼ばない |
| `OPENAI_API_KEY` | OpenAI API認証（統合AI⑤） | 設定済み |
| `FOODCOURT_OPENAI_MODEL` | 統合AI⑤のモデル名 | `gpt-5.6-luna` |

**質問による切り替え（2026-08-04 追加）**: Q&Aは1回$0.09かかるため、全質問で検索すると無駄が大きい。journal 側で実装・テスト済みの意図分類（`classifyJournalChatIntent`）を流用し、`data`（事実照会）と判定された質問では**検索そのものを行わない（課金ゼロ）**。`strategy` / `mixed` のときだけ走る。

| 質問例 | 判定 | X検索 |
|---|---|---|
| 直近で一番売れた日は？ | `data` | 走らない |
| 売上を伸ばすための提案を3つ | `mixed` | 走る |
| 今後の運営、世の中のトレンド… | `mixed` | 走る |

定期サマリー（日次/期間/週次）は質問が無いので、常に固定トピックで走る（キャッシュ共有で1日1検索）。

**検索対象の考え方（2026-08-04 変更）**: 当初は「東京ドーム／東京ドームシティの来場者の話題」に絞っていたが、**この範囲はX上の投稿数が足りず拾える話題が痩せる**。会場のイベント・客層は専門AI②が `tokyo_dome_events` から取得しており、Xで補う必要がない。Xにしか無いのは「いま何が食べられ、どう見せられ、どう語られているか」なので、地域を東京〜全国に広げ、**メニュー・商品の方向性に効く食トレンド**を主対象にしている（①スパイスカレー・カレー全般 ②ワインの飲まれ方 ③フードコート/商業施設内飲食の人気メニュー ④SNSでの見せ方・季節需要）。`max_tool_calls` の範囲で観点ごとに複数回検索する。

### フォールバック優先順位

```
統合AI⑤（openai preferred）: openai → gemini → groq
専門AI②（gemini preferred）: gemini → groq
反証AI④（gemini preferred）: gemini → groq
専門AI③（grok preferred）  : grok → gemini → groq
専門AI①（groq preferred）  : groq(openai/gpt-oss-120b) → gemini
評価AI⑥（groq preferred）: groq → gemini（共有期限内）
Xトレンドブリーフ③'        : grok(x_search) → フォールバック無し（取得できなければブロックごと省略）
```

Xトレンドブリーフだけはフォールバック先を持たない。他モデルは X を検索できないため、
代役を立てると「検索していないトレンド」が混入してしまう。取得失敗時は
プロンプトからブロックごと落とし、統合AIには「ブロックが無い場合はトレンドに言及しない」と指示している。

---

## 10. 旧構成のコスト概算（履歴専用）（2026年7月時点）

### モデル単価

| AI | プロバイダー | モデル | 入力/1M | 出力/1M |
|---|---|---|---|---|
| 専門AI① | Groq | openai/gpt-oss-120b | （Groq GPT-OSS 単価。`ai-usage.html` 参照） | 同左 |
| 専門AI② | Google | gemini-3.5-flash | $1.50 | $9.00 |
| **専門AI③** | **xAI** | **grok-3-mini** | **$0.30** | **$0.50** |
| 反証AI④ / 評価AI⑥ | Anthropic | claude-haiku-4-5 | $1.00 | $5.00 |
| 統合AI⑤ | OpenAI | gpt-5.6-luna | $1.00 | $6.00 |
| Xトレンド③' | xAI | grok-4.5 + x_search | トークン課金＋ x_search 実行料（別建て） | — |

### 月額試算（約55回/月のパイプライン実行）

| コンポーネント | 月額 |
|---|---|
| Groq 専門AI① | 約$0.12 |
| Gemini 専門AI② | 約$0.50 |
| **Grok 専門AI③** | **約$0.08** |
| **Grok Xトレンドブリーフ③'** | **約$0.5（トークンのみ・下記注意）** |
| Claude 反証AI④ | 約$0.37 |
| OpenAI 統合AI⑤ | 約$0.97 |
| **合計** | **約$2.5〜（約380円〜）** |

※ 単価は変動するため、実測の集計は `ai-usage.html` を正とする。

※ **Xトレンドブリーフの費用内訳（2026-08-04 本番実測）**: `grok-4.5` は入力$2.00／出力$6.00 per 1M（`grok-3-mini` の約7〜12倍）。さらに **`x_search` はトークンとは別に $5 / 1,000回（$0.005/回）** が課金される。

初回実測（検索1回）:

| 内訳 | 実測 | 割合 |
|---|---|---|
| 入力 31,728 tok × $2.00/1M | $0.0635 | **83%** |
| 出力 1,339 tok × $6.00/1M | $0.0080 | 10% |
| x_search 1回 × $0.005 | $0.0050 | 7% |
| **合計** | **$0.0765/回** | |

**費用の主成分はツール実行料ではなく入力トークン**。x_search が取得したX投稿の本文が丸ごとコンテキストに入るため、1検索あたり3万トークン規模で膨らむ。したがって効くレバーは検索回数（`GROK_X_SEARCH_MAX_TOOL_CALLS`）とキャッシュヒット率であり、いずれも「取り込む投稿量」を通じて入力トークンを左右する。

**実費の算出（ツール実行料込み）**: ブリーフのトークンは `ai_usage_events` に記録され、`model` が `grok-4.5 x_search*2` の形になっている（ツール回数の列が無いため model 名に残している）。次のSQLで実費が出る:

```sql
select
  count(*) as briefs,
  sum((regexp_match(model, 'x_search\*(\d+)'))[1]::int) as x_search_calls,
  round(sum(input_tokens) * 2.00 / 1e6 + sum(output_tokens) * 6.00 / 1e6, 4) as token_usd,
  round(sum((regexp_match(model, 'x_search\*(\d+)'))[1]::int) * 0.005, 4) as tool_usd,
  round(sum(input_tokens) * 2.00 / 1e6 + sum(output_tokens) * 6.00 / 1e6
        + sum((regexp_match(model, 'x_search\*(\d+)'))[1]::int) * 0.005, 4) as total_usd
from ai_usage_events
where provider = 'grok' and model like '%x_search%'
  and created_at >= date_trunc('month', now());
```

AI使用料ページ（`/usage/ai-cost`）はトークン費のみを表示する（`grok-4.5` 用の単価を別枠で持たせてあるので、`grok-3-mini` 単価で過小表示されることはない）。**ツール実行料はページに含まれない**ため、総額は上のSQLで確認する。

月額は $0.0765 × 検索回数。全サーフェスが同じ固定トピックを共有するため、キャッシュが効けば **1日1検索 = 月24〜30回 ≒ 月$1.8〜2.3**。キャッシュはisolate内メモリなので、コールドスタートが多いと回数は増える。高い場合は `FOODCOURT_X_SEARCH_CACHE_TTL_MS` を延ばす、`GROK_X_SEARCH_MAX_TOOL_CALLS` を減らす、`FOODCOURT_X_SEARCH_ENABLED=false` で停止、の順に効く。

**「検索したのに使われなかった率」の測定**: ブリーフが破棄された場合、model に `drop:<理由>` が付く（例 `grok-4.5 x_search*1 drop:missing_x_citations`）。課金は発生しているので、この比率が高いままなら設定を見直すか停止する。

```sql
select case when model like '%drop:%' then split_part(model, 'drop:', 2) else '(used)' end as outcome,
       count(*) as n,
       round(sum(input_tokens) * 2.00 / 1e6 + sum(output_tokens) * 6.00 / 1e6
             + sum((regexp_match(model, 'x_search\*(\d+)'))[1]::int) * 0.005, 4) as usd
from ai_usage_events
where provider = 'grok' and model like '%x_search%'
group by 1 order by n desc;
```

---

## 11. 廃止予定モデルの管理

| モデル | 廃止予定 | 移行先 |
|---|---|---|
| `gemini-2.5-flash` | 2026年10月16日 | `gemini-3.5-flash` ✅（設定済み） |
| `gemini-2.5-flash-lite` | 2026年10月16日 | `gemini-3.1-flash-lite` |

**モデル名は環境変数で管理しているため、廃止時はコード変更なしでSupabase Secretsの値を変えるだけで対応可能。**

---

## 12. 分析の限界と注意事項

### AIへの制約（プロンプトで強制）

- **表の値の言い換え禁止**: 「売上は¥82,000、FC平均の61%、3位です」のような羅列は禁止
- **捏造禁止**: データにない指標（推定来館者数・販売点数・原価）は「データにありません」と明示
- **因果の誤用禁止**: 相関（Pearson r）は因果ではないことを明示

### データの制限

- **前年比なし**: MARUGO S は新規オープン（2026年〜）のため前年同月比は計算不可
- **学習初期の精度**: データ増加だけでは改善を保証しない。事前予測を同条件で評価し、少数条件の係数は参考とする
- **イベント動員数**: 手入力の事前推定と取得済みの実測を区別する。現行5方式は動員数を予測変数に使用しない

---

## 13. 今後の改善候補

| 優先度 | 改善内容 | 期待効果 |
|---|---|---|
| 高 | 休業・営業時間短縮・警報など、発行前に取得できる入力の整備 | 台風等の誤差原因を切り分ける材料。現行には未実装 |
| 高 | 品質評価ループ（§3-3）の有効化と閾値チューニング | 回答品質の底上げ（まずQ&Aのみの段階導入を推奨） |
| 中 | 会場×客層別の係数（野球/ライブ/格闘技） | 業態相性の予測精度向上 |
| 中 | 客単価のイベント別係数化 | 売上予測の精度向上 |
| 低 | 複合条件・非線形モデルを新IDの候補として追加 | 改善を仮定せず、事前予測で現行・同曜日基準と比較 |
| 低 | Gemini 3.5 Flash → 3.1 Pro へのアップグレード | イベント分析の深度向上（月+$0.7程度） |

---

## 14. 関連ファイル

数値予測の正本: [FOODCOURT-FORECAST-AUDIT.md](./FOODCOURT-FORECAST-AUDIT.md)、実装: `supabase/functions/_shared/foodcourt_forecast_engine.ts`。

| ファイル | 役割 |
|---|---|
| [`supabase/functions/_shared/foodcourt_compare.ts`](../supabase/functions/_shared/foodcourt_compare.ts) | AI分析パイプライン本体（全5エージェント・コンテキスト構築） |
| [`supabase/functions/foodcourt-forecast-cron/index.ts`](../supabase/functions/foodcourt-forecast-cron/index.ts) | 来客予測モデルの学習エンジン・係数の保存 |
| [`supabase/migrations/20260703170000_foodcourt_forecast_factors.sql`](../supabase/migrations/20260703170000_foodcourt_forecast_factors.sql) | 学習係数保存テーブルの定義 |
| [`supabase/functions/admin-api/index.ts`](../supabase/functions/admin-api/index.ts) | AI分析APIエンドポイント・キャッシュ管理 |
| [`foodcourt.html`](../public/foodcourt.html) | フードコート分析画面（Q&A・日次/期間サマリー表示・動員数入力） |
| [フードコートAIループシステム全体解説.md](フードコートAIループシステム全体解説.md) | 2つの自己改善ループ（モデル選択＋品質評価）の全体像・有効化手順 |
| [AI_LOOP_ENGINEERING_DESIGN.md](AI_LOOP_ENGINEERING_DESIGN.md) | 品質評価ループの設計書・実装ログ（Phase 1〜3） |
| [フードコート学習システム構造.md](フードコート学習システム構造.md) | データ収集〜予測モデルの全体構造 |
| [フードコート売上分析_設計書.md](フードコート売上分析_設計書.md) | 分析フレームワーク詳細 |
| [フードコート競合店プロファイル.md](フードコート競合店プロファイル.md) | 競合11店の業態プロファイル |
