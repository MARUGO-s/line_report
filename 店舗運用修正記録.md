# MARUGO 店舗システム 運用・修正記録

LINE 売上／レシート管理システム（22店舗）で発生した不具合と、その対策を記録する運用ログです。
**新しい修正を行ったら「修正履歴」の先頭（新しいものを上）に追記**してください。

---

## このファイルの目的
- どの店舗で何が起き、どう直したかを後から追えるようにする。
- 同種の不具合が再発したとき、前回の原因・対策をすぐ参照できるようにする。

## 追記テンプレート（コピーして使用）

```
### YYYY-MM-DD 件名
- 対象店舗 / 範囲:
- 症状（不都合）:
- 原因:
- 対策（変更内容）:
- 反映: コミット <hash> / migration <file> / 関数デプロイ <name>
- 検証:
```

---

## 店舗一覧（store_partition_key / 表示名 / receipt_table）

| store_partition_key | 表示名 | receipt_table |
|---|---|---|
| marugo | マルゴ | line_receipt__marugo |
| marugoD | マルゴ D | line_receipt__marugoD |
| marugoS | マルゴエス | line_receipt__marugoS |
| marugosecond | マルゴ セカンド | line_receipt__marugosecond |
| marugogrande | マルゴ グランデ | line_receipt__marugogrande |
| marugootto | マルゴ オット | line_receipt__marugootto |
| marugomarunouchi | マルゴ丸の内 | line_receipt__marugomarunouchi |
| marugoshinbashi | マルゴ 新橋 | line_receipt__marugoshinbashi |
| marugoyotsuya | マルゴ 四谷 | line_receipt__marugoyotsuya |
| yakinikumarugo | 焼肉マルゴ | line_receipt__yakinikumarugo |
| bistrocavacava | ビストロ サヴァサヴァ | line_receipt__bistrocavacava |
| barpelota | バルぺロタ | line_receipt__barpelota |
| briccola | トラットリア ブリッコラ | line_receipt__briccola |
| claudia2 | クラウディア2 | line_receipt__claudia2 |
| shenlong | シェンロン&クラウディア | line_receipt__shenlong |
| violette | ヴィオレット | line_receipt__violette |
| sauvage | ソバージュ | line_receipt__sauvage |
| sannanaichi | サンナナイチ バル | line_receipt__sannanaichi |
| donaiya | 元祖どないや 新宿三丁目店 | line_receipt__donaiya |
| erics | エリックスバイエリックトロション | line_receipt__erics |
| mitan | ミタン | line_receipt__mitan |
| sushikoruri | 鮨こるり | line_receipt__sushikoruri |
| （admin） | LINE利用許可（承認専用・実店舗ではない） | line_receipt__admin |

> 実店舗は 22、`admin` は承認専用 Bot。

---

## システム構成メモ（デバッグの前提知識）

- **レシート本体**: 店舗ごとの `line_receipt__<key>` テーブル（売上データの正本）。
- **売上検索**（LINE の「売上検索結果」カード）: 本体ではなく**検索インデックス `line_room_receipt_search`** を参照する（本体とは別物）。**本体を削除/置き換えたらインデックスも同期削除が必要**。同期漏れがあると「幽霊行（本体に無いのに検索に出る）」が発生する。
- **月予算**: `line_sales_month_budgets`。**手入力月総額** `line_sales_manual_month_gross` があると自動集計（レシート合算）を上書きする。
- **天候**: `line_weather_daily`（恒久保存）。Open-Meteo を使用。過去は ERA5 アーカイブで **1940 年まで**遡れる（直近約5日は予報APIで補完）。取得範囲は分析画面で選んだ期間に依存。
- **国民の祝日**: 内閣府 `syukujitsu.csv` をサーバ取得（`admin-api /analytics/holidays`、12h キャッシュ、取得失敗時はハードコード表へフォールバック）。
- **レシート解析**: Groq ビジョン（`receipt_vision.ts`）。既定プロンプトは `receipt_prompt.ts` の定数。店舗別の補足は `store_receipt_analysis_prompts`（管理画面＞店舗設定モーダル＞レシート関連で編集）に保存し、解析時に既定へ連結。未設定店は既定のまま。
- **Webhook の再送**: `webhook_event_id` でべき等化（同一IDの再送はスキップ）。これが無いと重複返信が起きる。
- **権限/機能トグル**: `room_summary_settings`（bot_reply_hard_mute_enabled、image_analysis_reply_enabled、receipt_correction_reply_enabled、non_receipt_image_reply_enabled 等）。
- **ルームメディア（メディア閲覧）**: メタは `line_message_media`、本体は `line-media`（private バケット）。Webhook が受信メディア（画像/動画/音声/ファイル）を保存し、**1ルーム合計 20MB を超えると古い順に自動削除**（`_shared/line_media_store.ts`、`ROOM_MEDIA_CAP_BYTES`）。会話検索用の `line_room_media_search`（メタのみ・本体なし）とは別物。デプロイ前に届いた分は LINE 本体 URL 期限切れのため遡及保存不可。
- **予約の店舗振り分け**: 予約は1つの Gmail に集約され、AI が `storeName` を抽出。カレンダー保存用 `reservation_detail`(JSON) に格納し、admin-api `/reservations/calendar` が `store_name` で返す。予約表は `resolveStoreKey`（pages-config、敬称 様/御中・大小文字・空白の表記ゆれ吸収）で22店舗マスターへ正規化。一致しないものは「未分類」。**店休日表示は未対応**。
- **LINE 経由アクセスの制限**: `?from=line` または LINE アプリ内ブラウザ UA を検知すると**売上分析ページのみ許可**。他ページ（管理コンソール/メディア/予約/会話検索）は `<head>` 早期に売上分析へ強制リダイレクト（店舗キーは `line_locked_store`＝session/localStorage で保持）。通常ブラウザは影響なし。
- **デプロイ手順**:
  - フロント（`index.html` / `analytics.html`）: GitHub Pages へ `git push origin main`。
  - サーバ関数: `supabase functions deploy <name> --project-ref hocbnifuactbvmyjraxy`（主要関数: line-webhook / admin-api / receipt-sheets-sync-cron）。
  - DB: 新規 migration を `supabase/migrations/` に追加 → `supabase db push --linked`（`--dry-run` で保留分を事前確認）。

---

## 修正履歴

### 2026-06-03 売上分析: 日次売上を表セルから直接インライン編集できる機能を追加（総売上・組数・客数／全店）
- **対象 / 範囲**: 売上分析の「日次売上（数値）」表。各日の **総売上・組数・客数** をセルクリックで直接編集（列ごとに個別上書き）。上書きした日はその日のレシート集計より優先し、**月間合計（KPI）・日別合計・12ヶ月トレンドにも反映**。全店。
- **背景 / 要望**: 「売上分析画面から、直接日次売上の編集をできるようにしてほしい」。モーダルではなく**表の中で直接インライン編集**、編集項目は**売上・組数・客数すべて**（要望で確定）。
- **データ設計**: 新テーブル `public.line_sales_manual_day`（`store_partition_key`＋`sales_date` で一意。`gross_sales_yen`/`party_count`/`guest_count` は **NULL可**＝列ごと個別上書き、`>=0 or NULL` チェック制約、RLSは service_role のみ）。値が入っている**列だけ**その日のレシート集計を上書きし、未入力(NULL)列はレシートへフォールバック。3列すべてNULLになったらアプリ側で**行ごと削除**しその日を完全にレシート集計へ戻す。
  - 月次手入力（`line_sales_manual_month_*`）とは別系統。**月全体の上書きより日次上書きが細粒度で優先**：日次手入力がある月は月全体の上書きを適用しない（レシート優先=Option A とも整合）。
- **内容（変更）**:
  1. **DB**: migration `20260603150000_create_line_sales_manual_day.sql`（テーブル＋`sales_date desc` 索引＋RLSポリシー）。
  2. **共有ヘルパー** `_shared/manual_day_sales.ts`: 取得 `fetchManualDaySalesMapForStore`（日付キーのMap）／upsert `upsertManualDaySalesEntries`（read-modify-writeで既存値とマージ。undefined=据置・null=解除・数値=上書き。全NULLは行削除）。
  3. **admin-api**: `PUT /receipts/sales-manual-days` を追加（`{ store_key, entries:[{ sales_date, gross_sales_yen?/party_count?/guest_count? }] }`。送られた列だけ更新）。
  4. **集計** `admin_receipt_sales.ts`: 日別 `series` を「レシート集計 × 日次手入力」で列ごとマージし、`manual_gross`/`manual_party`/`manual_guest` のフラグを各日に付与。月間 `totals`（KPI）は**マージ後の日別合計**で算出。`fetchAnalyticsMonthly`（12ヶ月トレンド）も日次手入力の差分を月バケットへ反映し整合。
  5. **フロント** `analytics.html`: 日次表の総売上・組数・客数セルをクリック→`<input>` に置換して編集（Enter/フォーカス外で確定・Escapeで取消・空欄で上書き解除）。確定時に `PUT /receipts/sales-manual-days` 後 `loadAll()` で再読込。手入力中の列は**色＋✎印**（`rd-manual`）で表示。編集中フラグ `receiptDailyEditActive` でビューポート再描画による入力消失を抑止。変更なし（受領値のまま）の確定はAPI無送信＝レシートセルの誤上書きを防止。
- **トレードオフ / 補足**: 日次手入力はGoogleシート連携には反映されない（DB側のみ。シート連携 `receipt_sheets_pilot_sync.ts` とは独立で競合なし＝許容）。レシート優先(Option A)で「月別売上を入力」の月次上書きが効かない月でも、**日次インライン編集なら個別日を上書き可能**。
- **反映（本番・hocbn `hocbnifuactbvmyjraxy`）**: migration `20260603150000` を `supabase db push` で適用（残1件＝本テーブルのみ・既存集計に影響なし＝手入力0件時はレシートへフォールバックの無変化）。関数デプロイ `admin-api`。フロント `analytics.html` は GitHub Pages（`MARUGO-s/line_report`）へ commit & **push 必要**。
- **検証**: `db push` で `20260603150000` 適用成功（remote未適用はこの1件のみと `migration list` で確認）。`admin-api` デプロイ成功（script 246.5kB）。`analytics.html` インラインJS構文0エラー（`node --check`）。**ダッシュボード再読込→日次表の総売上/組数/客数セルをクリックで編集でき、確定後に当該セル＝色+✎・月間合計と日別合計へ反映されることを要確認**（空欄確定で解除＝レシート値へ戻る）。

### 2026-06-03 売上分析: 月間合計を「レシート優先」に変更（手入力月次の古い/部分値が実売上を上書きする不具合の根治／全店）
- **対象 / 範囲**: 売上分析の月間合計（総売上・組数・客数）と12ヶ月トレンド。`admin_receipt_sales.ts` の `mergeSalesTotalsWithManualMonth`（KPI合計）と `fetchAnalyticsMonthly`（トレンド）。全店。
- **症状**: 直前の「gross=0 掃除」では直らない残存ケース。例: ソバージュ(`sauvage`) 2026-06 は日別が 6/1 ¥30,709 + 6/2 ¥97,200（合計 ≈¥127,909）なのに**月間総売上 ¥30,709**・「月計手入力あり」表示。`gross_sales_yen=0` ではない**古い/部分的な手入力月次値**が実売上を上書きしていた。
- **原因**: 月間合計は手入力月次グロスがあると**無条件でレシート集計より優先**する旧仕様。シート連携が「日次の途中集計（6/1 だけ等）」を月次手入力レコードとして自動生成すると、後から届くレシート(6/2 分)が反映されず、手入力側の古い値で月間が固定される。gross=0 に限らず gross>0 の部分値でも同種の食い違いが起きる（掃除では根治できない）。
- **対策（レシート優先 / Option A）**: マージ方針を反転。**レシートが1件でもある月は合計を必ずレシート集計（=日別の合計）にする**。手入力の月次上書きは「レシートが1件も無い月」だけ有効。
  1. `mergeSalesTotalsWithManualMonth`: `receipt_count>0` ならレシート集計をそのまま返す（手入力を無視）。
  2. `fetchAnalyticsMonthly`: 月バケットに `receipt_count>0` があればその月の手入力上書きをスキップ（KPIとトレンドの整合）。
  3. `manual_month_has_entry`: 手入力が**実際に合計へ反映されている時**（=`receipt_count===0`）のみ true に。誤った「月計は手入力」表示を抑止。
- **トレードオフ（許容済み）**: レシートがある月は「月別売上を入力」での総売上・組数・客数の手入力上書きが**効かなくなる**（=レシート集計が正本）。手入力上書きは前年同月比など前年の手入力データ（`receipt_reply_context.ts` / `receipt_report_flex.ts`）には影響なし＝従来どおり手入力が正本。
- **反映（本番・hocbn `hocbnifuactbvmyjraxy`）**: 関数デプロイ `admin-api` のみ。**データ削除なし**。フロントは `manual_month_has_entry` 連動の表示が自動で正しくなるため変更不要。
- **検証**: ダッシュボード再読込で**ソバージュ 2026-06 が日別合計（≈¥127,909）と一致**し「月計は手入力」が消えることを要確認。他店も日別合計＝月間合計になる見込み。

### 2026-06-03 売上分析: 月間総売上が ¥0 と誤表示される不具合を修正（手入力月次グロスの汚染／全店）
- **対象 / 範囲**: 売上分析の月間合計（総売上・組数・客数）。`line_sales_manual_month_gross` に汚染レコードがあった店舗。今回掃除したのは briccola・marugo・marugootto・marugosecond・sushikoruri・bistrocavacava・claudia2 の 2026-06 と yakinikumarugo の 2026-05（計8件）。
- **症状**: 日別グラフ・日別行はレシート通りの売上が出る（例: ビストロ サヴァサヴァ 6/2 ¥105,000、組数2/客数8）のに、**月間総売上だけ ¥0**。組数・客数は正しいまま。複数店で発生し「合計の計算がおかしい」状態に見えた。
- **原因**: 月間合計は手入力月次グロスがあるとレシート集計より優先する設計（`analytics.html` 注記／`admin_receipt_sales.ts` の `mergeSalesTotalsWithManualMonth`）。`gross_sales_yen=0` のレコードがあると総売上が 0 で**無条件上書き**される一方、組数・客数は未入力だとレシートにフォールバックするため「総売上だけ ¥0」になる。`manualMonthSalesFromRow` は 0 を有効値として弾かない（`!Number.isFinite || <0` のみ除外）。日別系列はレシートのみで算出するため日別は正しく出る。
  - **0レコードの発生源**: シート連携の取込 `pullDailySheetEditsToManualMonthGross`（`prefer_sheet_on_content_diff=true` の pull/both 時のみ動作・呼出し箇所 line 1030）が、日次シートのゼロ埋め行を月次集計して `gross=0` の手入力レコードを各店に書き込んでいた。
- **対策**:
  1. **取込側** `receipt_sheets_pilot_sync.ts`: 月次集計 `gross<=0` のとき `gross_sales_yen:null` を渡し該当行を**削除**（=レシート集計へフォールバック／0を残さない）。あわせてシートのオールゼロ行を「自動ゼロ埋め」とみなし月次反映・日次保持の双方でスキップ。
  2. **データ掃除（本番）**: 残存する `gross_sales_yen=0` の手入力月次レコードを2段階で全削除（第1弾=party/guest/opdays全NULL、第2弾=残り全て）。計8件。削除は破壊的だが gross=0 はレシートがある月では常に誤上書きで、削除＝手入力優先の解除（レシート正本へ復帰）のため安全。`opdays` 同梱分（bistrocavacava=22）も削除されるが営業日数KPIはレシート稼働日数を優先（手入力opdaysはレシート0日時のみ使用）のため影響なし。
- **反映（本番・hocbn `hocbnifuactbvmyjraxy`）**: migration `20260603130000_cleanup_spurious_zero_manual_month_gross.sql` ／ `20260603140000_cleanup_remaining_zero_manual_month_gross.sql` を `supabase db push` で適用（各 NOTICE で削除内容を監査ログ出力）。関数デプロイ `receipt-sheets-sync-cron`（取込側修正）。admin-api・フロントは変更不要（0レコードが消えれば既存ロジックがレシート集計を返す）。コミット（本記録と同コミット）。
  - **補足（GAS・任意/未適用）**: `google-apps-script/receipt-sheets-pilot/Code.gs` の自動バックグラウンド同期から `prefer_sheet_on_content_diff:true` を外す変更が未コミットで残っている。反映すると自動同期がDB正本になり再汚染をトリガ段階でも止められる（サーバ側修正だけでも再発はしない）。GASは別経路デプロイのため本コミットには含めず、要否は別途判断。
- **検証**: migration NOTICE で削除8件と対象店舗・月を確認（第1弾6件＋第2弾2件、bistrocavacava/2026-06 を含む）。掃除後は手入力レコードが無く `mergeSalesTotalsWithManualMonth` がレシート集計をそのまま返すため月間総売上が日別合計と一致する見込み。**ダッシュボード再読込で総売上が ¥0 でなくなることを要確認**。

### 2026-06-03 Gmail予約通知: キャンセル時に来店回数が -1 されない不具合を修正（食べログ／一休）
- **対象 / 範囲**: Gmail予約通知（食べログ・一休）の来店回数集計。全店共通。
- **症状**: キャンセルメールを受信しても対象顧客の `visit_count` が -1 されず、むしろ +1 される（または集計対象から外れる）ケースがあった。「新規で +1、キャンセルで -1」のうち -1 が実質発火していなかった。
- **原因**: `gmail-alert-cron` が集計RPC `record_{tabelog,ikyu}_reservation_visit` に渡す引数にキャンセル種別が含まれていなかった。
  - `p_reservation_type` は `inferReservationTypeLabel(plan, seatName)` 由来で `course`/`seat_only`/`unknown` の3値のみ（キャンセル文言を含まない）。
  - `p_reservation_detail`(JSON) は `route/plan/storeName…` のみで、DB側 `reservation_visit_looks_cancelled` が見る `eventType`/`action`/`mailType` 等のキーを出力していなかった。
  - そのため判定が常に false → +1 ブランチに入り、-1 ブランチ（`visit_count = greatest(0, visit_count-1)`）はデッドコード化。
  - キャンセル判定自体は `inferReservationEventLabel(subject, snippet)`（→「予約キャンセル」）で行われていたが、**LINE通知の見出し【】表示専用**で集計RPCには渡っていなかった（検知と集計の断絶）。
- **対策**: `buildReservationCalendarDetailPayload` が detail JSON に `eventType`（"予約キャンセル"/"予約変更"、新規予約は null）を載せるよう producer 側を1箇所だけ修正。これによりDBがキャンセルを検知し -1 が発火する。
  - 同じ `eventType` キーは `admin-api`（予約表のキャンセル判定 `reservationParsedDetailLooksCancelled`）と `reservation-today-cron`（本日の予約状況からキャンセル除外 `looksCancelled`）も参照済みのため、**表示側のキャンセル検知も同時に改善**。DB/admin-api/today-cron 側の変更・マイグレーションは不要。
- **前提条件（残課題）**: RPCは氏名＋電話が両方抽出できた時のみ動作し、元予約と氏名＋電話が一致する集計行を -1 する。キャンセルメールから氏名・電話が取得できない／表記が揺れる場合は減算されない。
- **反映（本番・hocbn `hocbnifuactbvmyjraxy`）**: コミット（本記録と同コミット）／ `gmail-alert-cron` 再デプロイ済（`verify_jwt` 既定=true を維持）。DBマイグレーション無し・フロント変更無し。
- **検証**: `deno check` で本変更箇所に型エラー0（既存19件はAI抽出・型定義の既存エラーで本変更と無関係）。実キャンセルメールでの -1 反映は次回キャンセル受信時に `visit_count` で要確認。

### 2026-06-03 「明日の予約状況配信」を「本日の予約状況配信」へ時間軸変更（tomorrow→today に完全リネーム）
- **対象 / 範囲**: 全店。直前に追加した予約まとめ配信の対象日を「翌日」→「当日」へ変更。送信タイミング（ルーム個別の時・分）は不変。
- **背景 / 要望**: 「明日の予約」ではなく「本日の予約」をまとめて配信したいとの要望。動作・表示・内部名称すべて today へ統一（部分対応ではなく完全リネーム）。
- **内容（変更）**:
  - DB列 `tomorrow_reservation_alert_{enabled,hour,minute}` → `today_reservation_alert_*` にリネーム。ログテーブル `reservation_tomorrow_alert_logs` → `reservation_today_alert_logs`（索引・一意制約・RLSポリシー・コメントも追随）。
  - Edge Function `reservation-tomorrow-cron` → `reservation-today-cron`（ディレクトリごと改名）。対象日抽出を翌日(JST)→当日(JST)へ変更（`jstTomorrowParts` を廃止し当日の `toJstDateParts` を使用）。
  - pg_cron: `reservation-tomorrow-cron-job` / `invoke_reservation_tomorrow_cron()` を撤去し、`reservation-today-cron-job` / `invoke_reservation_today_cron()`（毎分・当日URL）へ作り直し。
  - 配信文言: 「明日のご予約」→「本日のご予約」、予約0件は「本日のご予約はありません。」。
  - テスト送信: クエリ `?test_today_reservation=1`、ヘッダ `X-Reservation-Today-Test-Key`、Edgeシークレット `RESERVATION_TODAY_CRON_TEST_KEY` へ改名（旧キーを設定済みなら再設定が必要）。
  - `admin-api` / `index.html` の入出力フィールド・要素ID・localStorageキーも today へ統一。
  - ※ 別機能「明日予定配信（`calendar_tomorrow_reminder_*` / リマインダー）」は対象外・一切不変。
- **反映（本番・hocbn `hocbnifuactbvmyjraxy`）**:
  - migration `20260603120000_rename_tomorrow_to_today_reservation_alert.sql`（列/テーブル/索引/制約/ポリシー/cron/invoke関数を today へ）を `supabase db push` で適用済。
  - 関数デプロイ `reservation-today-cron`（新規）／旧 `reservation-tomorrow-cron` は削除済／`admin-api` 再デプロイ済／`config.toml` を `reservation-today-cron`・`X-Reservation-Today-Test-Key` へ更新。
  - `index.html` を GitHub Pages（`MARUGO-s/line_report`）へ commit & push。
- **検証（実施済）**: `reservation-today-cron` を空ボディ起動 → `today_*` 列を含む `room_summary_settings` の SELECT 成功（HTTP 200 / `no_rooms_scheduled_now`）で列・関数・cron整合を確認。関数一覧から旧 `reservation-tomorrow-cron` 消滅・`reservation-today-cron` のみ ACTIVE を確認。`index.html` はインラインJS 6ブロック構文0エラー、旧 `明日`/`tomorrow_reservation_alert` 系の残存0・別機能リマインダーの参照数不変を確認。有効化ルームは0件のため誤送信なし。
- **残る手動確認**: 対象ルームを ON にし送信時刻を1〜2分後に設定 → 該当時刻に「本日のご予約」1通が届くこと、予約0件で「本日のご予約はありません。」が届くことを確認してください。

### 2026-06-03 新機能「明日の予約状況配信」を追加（Gmail予約通知とは別権限・1日1回まとめてリッチ配信）
- **対象 / 範囲**: 全店。ルーム設定（権限）に新トグル「明日の予約状況配信」を追加。有効化したルームのみ対象。
- **背景 / 要望**: 既存の「Gmail予約通知（`gmail_reservation_alert_enabled`）」は予約が入るたびリアルタイムに1件ずつ通知する仕組み。これとは別に、「明日の予約状況」を**1日1回・指定時刻**に、予約ごとのWebhook分割ではなく**1通のリッチテキスト（Flex）にまとめて**配信したいという要望。
- **内容（新機能）**:
  - ルームごとに ON/OFF と送信時刻（時:分）を設定可能（既定 18:00、未設定ルームは18:00継承）。送信時刻はルーム個別 or 一括（店舗/全体）で指定。
  - 対象予約は**店舗単位**で抽出（ルームの `receipt_report_store_partition_key` と予約メールの店名を照合）。キャンセル扱いの予約は除外。
  - 予約が0件の日は「明日のご予約はありません。」を送信（無通知にはしない）。
  - 食べログ（`tabelog_reservation_visit_events`）と一休（`ikyu_reservation_visit_events`）の翌日(JST)来店予約を1度だけ取得し、各ルームの店舗で絞り込んで時刻順に整列。
  - 二重送信防止: `reservation_tomorrow_alert_logs`（`room_id`×`target_date` のユニーク制約）に送信前へ行を確保。送信失敗時は行を取り消して次回起動で再送。
  - 店舗別 LINE トークン（`LINE_CHANNEL_ACCESS_TOKEN__<KEY>`）に対応。テスト送信は `?test_tomorrow_reservation=1&room_id=…`（ヘッダ `X-Reservation-Tomorrow-Test-Key`、ログ非記録のプレビュー送信）。
- **仕組み**: pg_cron が**毎分** `reservation-tomorrow-cron` を起動し、関数側で「今この瞬間(JST)に送るべきルーム（時刻一致＋トグルON＋ルーム有効）」を判定して送信（既存の `receipt-midreport-cron` と同方式）。
- **反映**:
  - **本番デプロイ完了（2026-06-03・hocbn `hocbnifuactbvmyjraxy`）**: `supabase db push` で2マイグレーション適用済（remote 記録確認）／`reservation-tomorrow-cron`・`admin-api` デプロイ済／`index.html` を GitHub Pages（`MARUGO-s/line_report`）へ commit & push 済・配信確認。
  - migration `20260603100000_tomorrow_reservation_alert_columns.sql`（`room_summary_settings` に3列追加 + `reservation_tomorrow_alert_logs` テーブル）
  - migration `20260603110000_reservation_tomorrow_cron_every_minute.sql`（pg_cron 毎分起動）
  - 関数デプロイ `reservation-tomorrow-cron`（新規）／ `config.toml` に `verify_jwt = false` 追記
  - `admin-api`（`/settings/rooms` の新フィールド検証・upsert・返却）／ `index.html`（トグル＋時刻入力・権限/新規ルーム両モーダル・レビュー表示・送受信の往復配線）
- **検証（実施済）**: デプロイ直後に `reservation-tomorrow-cron` を空ボディで起動し、新3列を含む `room_summary_settings` の SELECT が成功（HTTP 200 / `no_rooms_scheduled_now`）→ 列・関数・スキーマ整合を確認。GitHub Pages 配信 `index.html` の新UIマーカー存在も確認。現時点で有効化ルームは0件のため誤送信なし。
- **残る手動確認**: 対象ルームを ON にし送信時刻を1〜2分後に設定→該当時刻に1通のリッチメッセージが届くこと、予約0件で「明日のご予約はありません。」が届くこと、同一時刻の二重起動でも1通のみであることを確認してください。

### 2026-06-03 GAS手動同期でタイムスタンプ無視のシート優先ロジックを削除
- **対象 / 範囲**: GAS `Code.gs`（`triggerServerBackgroundSync_`）。全店舗の手動同期フロー。
- **症状**: GASメニュー「⚡ 全店舗を同期」が常に `prefer_sheet_on_content_diff: true` を送っており、値の差異があればタイムスタンプに関係なくシートが無条件に勝っていた。本来の設計（タイムスタンプが新しい方が相手を上書き）と矛盾。
- **対策**: `prefer_sheet_on_content_diff: true` を削除。手動同期もスケジュール同期と同様、タイムスタンプ比較で新しいデータが相手を上書きする動作に統一。
- **反映**: `clasp push`（GAS `Code.gs` を直接デプロイ）。

### 2026-06-03 双方向同期で新規売上がシートに反映されない不具合を修正（日次売上タブ）
- **対象 / 範囲**: `receipt-sheets-sync-cron`（`_shared/receipt_sheets_pilot_sync.ts`）。全店舗の「日次売上」タブ。
- **症状**: DB（レシートテーブル）に当日・前日の売上データが入っているのに、「⚡ 全店舗を同期」を実行してもスプレッドシートの日次売上タブに売上が反映されない（ゼロのまま）。
- **原因**: `mergeDailyExportRowsPreservingSheetEdits` の `prefer_sheet_on_content_diff` ロジック。GAS メニューから同期すると常に `prefer_sheet_on_content_diff: true` が渡される。前回の自動同期でシートに書き込まれたゼロ（レシート未着時のゼロ埋め）を「手動編集」と誤認し、新しいDB値よりシートのゼロを優先して書き戻していた。同様に `pullDailySheetEditsToManualMonthGross` もシートのゼロ行を手動編集と見なし、`line_sales_manual_month_gross` の月次合計をゼロで上書きしていた。
- **対策**: 両関数に「シートの日付行が gross/party/guest すべてゼロ = 自動ゼロ埋め → 手動編集とみなさずDBデータを優先」チェックを追加。非ゼロのシート値（真の手動編集）は従来通り保持。
- **反映**: 関数デプロイ `receipt-sheets-sync-cron`（2026-06-03・全店共通）。
- **検証**: 変更後、GAS メニューから同期を実行して日次売上タブに実売上が反映されることを確認してください。

### 2026-06-02 テキストの「はい」等に店名不一致の案内が誤反応する不具合を修正（全店共通）
- **対象 / 範囲**: line-webhook（`_shared/receipt_store_mismatch.ts`）。全店共通のテキスト受信フロー。
- **症状**: 通常会話の「はい」（OK / 登録 / yes / 1 等）に対し、ビストロ サヴァサヴァ等のルームで「登録店舗と解析結果の店名が異なります。…解析結果の店舗『（解析店名）』に対応するWebhookが見つかりませんでした。…」と**誤返信**（店名は空＝プレースホルダのまま）。画像解析が走ったわけではない。
- **原因**: 旧「店名不一致 確認」フローの判定不備。`tryHandlePendingStoreNameMismatchConfirmation` が、**保留(pending)が1件も無いのに**確認語（はい 等）だけで案内文を送信していた（`if (!pending && !isLegacyRegisterAttempt) return null` が確認語を素通りさせていた）。`store_receipt_store_mismatch_pending` テーブルは現在 0 件。
- **対策**: 「**実際の保留(pending)が存在する時だけ**確認語に反応」へ変更（`if (!pending) return null` を先頭に）。保留が無ければ「はい」は通常会話として無視。画像解析時の**正規の店名不一致案内**には影響なし。
- **反映**: 関数デプロイ `line-webhook`（2026-06-02・全店共通）。
- **検証**: pendingテーブル 0 件を確認。`deno check` で本変更起因の新規エラーなし。デプロイ後 line-webhook 正常起動（`{"ok":false,...}` の入力検証応答）。

### 2026-06-02 LINE受信画像を「メディア閲覧」用に縮小＋再圧縮して保存（容量を約1/2.5〜1/3に）
- **対象 / 範囲**: line-webhook（`_shared/line_media_store.ts`）。メディア閲覧用に保存する**画像のみ**。
- **背景**: レシート画像が1枚あたり200〜450KB前後で、1ルーム20MB上限（FIFO）にすぐ達する。容量を抑えて保存枚数を増やしたい、という要望。
- **対策**: ストレージ保存の直前に、JPEGを**長辺1280px へ縮小＋画質75 で再圧縮**（純JS `jpeg-js`。WASM不要でEdgeバンドルに安全。imagescriptはWASMを実行時取得する作りのため不採用）。実測で **400KB級→約150〜170KB（約1/2.5〜1/3）**。20MB枠に約2〜3倍保存できる。
- **OCRへの影響：なし**。レシート解析は保存用とは別ルートで元画像を取得しているため、本圧縮は「メディア閲覧」保存コピーにのみ作用する。
- **安全策**: JPEGのみ対象（PNG等は素通り）／対象は **80KB〜8MB** のみ（極小は対象外、巨大はメモリ保護で素通り）／再圧縮が失敗 or 逆に大きくなる場合は**元データのまま保存**／`try/catch` で全面ガード（圧縮失敗が受信処理を止めない）。処理は約170〜230ms/枚。
- **反映**: 関数デプロイ `line-webhook`（2026-06-02）。**以後の受信画像に適用**（既存の保存分はそのまま）。
- **検証**: `jpeg-js` を Deno 実機で decode→縮小→encode 動作確認（実レシート 457KB→167KB 等）。`deno check` で本変更起因の新規エラーなし。デプロイ後 line-webhook は正常起動（`{"ok":false,...}` の入力検証応答＝ブート正常）。

### 2026-06-02 権限保存時の「スキップ件数」に、どのルームがスキップされたか（ルーム名一覧）も表示
- **対象 / 範囲**: index.html。Webhook設定 →「全体権限設定」の保存、および「店舗単位のWebhook設定」保存。
- **背景**: 保存時に「ルーム個別のためスキップ: N件」と**件数だけ**表示され、どのルームが（ルーム個別設定が優先のため）スキップされたか分からなかった。
- **対策**: スキップ対象ルームのラベル（`getRoomLabelFromRow`）を収集し、通知に**一覧表示**するよう変更。共通ヘルパ `appendSkippedRoomsNoticeLines`（最大20件まで列挙し、超過分は「ほかN件」）を新設し、全体権限保存（`saveGlobalPermissionFromModal`）と店舗単位保存の両方に適用。表示文言は「ルーム個別設定のためスキップ: N件」＋各ルーム名。
- **反映**: index.html（GitHub Pages）。※push で本番反映。
- **検証**: ローカルプレビューで再読込→初期化正常・コンソールエラーなし。スキップ時に件数＋ルーム名が出る。

### 2026-06-02 レポート送信が「ビストロ サヴァサヴァ以外の全店」でエラーになる不具合を修正（店舗別LINEトークン未使用）
- **対象 / 範囲**: receipt-midreport-cron（中間/月末レポートの**定期送信・テスト送信の両方**）。bcc 以外の全店で発生。
- **症状**: テスト送信（および定期送信）が、ビストロ サヴァサヴァ以外の店舗（例: バルペロタ）で**全部エラー**。bcc だけ成功する。
- **原因**: cron が全ルームに**既定トークン `LINE_CHANNEL_ACCESS_TOKEN` で push**していた。店舗ごとに LINE 公式アカウント（チャネル）が別で、**既定トークン＝bcc のチャネル**だったため、bcc のルームにしか送信が通らなかった（token digest: 既定 `5f307d49…` ＝ `__BISTROCAVACAVA`、`__BARPELOTA`=`00bd6357…`/`__MARUGO`=`f0c3a852…` は別物）。直前のレシート集計修正で bcc 以外も売上データを拾えるようになり、それまで「データ無し＝送信せずスキップ（無害）」で隠れていた送信失敗が、実際に push を試みて**エラーとして表面化**した。
- **対策**: cron に店舗別トークン解決 `resolveStoreLineToken`（`_shared/line_client.ts` の `resolveChannelAccessToken` と同じ env 命名 `LINE_CHANNEL_ACCESS_TOKEN__<店舗キー大文字>`、無ければ既定へフォールバック）を追加。`dispatchReceiptReport`（定期）と `handleReceiptReportTestSend`（テスト）の双方で、各ルームの店舗キーから解決したトークンで送信するようにした。
- **反映**: 関数デプロイ `receipt-midreport-cron`（2026-06-02）。
- **検証**: barpelota は per-store テーブルに 5/30=¥269,100・5/31=¥186,350 のデータあり（push 試行→従来は失敗）。デプロイ後、関数は到達(403)。バルペロタの月末テスト送信が当該店舗のチャネルから届く想定。
- **重要**: スケジュールの**定期レポートも同じ既定トークン依存**だったため、これまで bcc 以外には届いていなかった可能性が高い。本修正で全店復旧。

### 2026-06-02 レポート集計の「陳腐化テーブル依存」を構造的に除去（再発防止の仕上げ）
- **対象 / 範囲**: receipt-midreport-cron（集計ロジック）、admin-api（死にコード削除）。
- **背景**: 直前の修正でレポートは正本（店舗別テーブル）参照にしたが、`line_receipt_entries`（陳腐化した集約表）への参照が一部残っていた（cron のフォールバック＋admin-api の未使用関数）。同種の取りこぼし再発を構造的に断つため除去した。
- **対策①（cron）**: `loadReceiptReportAggregateForStoreByReceiptDate` を**「正本＝店舗別テーブルのみ参照」**に変更。`store_webhook_tables` レジストリ／対象テーブルが解決できない場合は `null`（＝スキップ・理由表示）を返し、**`line_receipt_entries` への集計フォールバックを撤去**。陳腐化表を読む補助関数（`loadReceiptRowsAnalyticsAligned` 等）も削除。方針は「間違うより出さない（古い数値で誤集計しない）」。
- **対策②（admin-api）**: `line_receipt_entries` を読む**未使用関数3つ**（`fetchReceiptStoreOptions` / `fetchReceiptSalesState` / `fetchAnalyticsMonthly` のローカル版・計408行）を削除。ルートは従来どおり `admin_receipt_sales.ts`（正本＝店舗別テーブル）参照のまま。**admin-api から `line_receipt_entries` 参照はゼロ**になった。
- **反映**: 関数デプロイ `receipt-midreport-cron` / `admin-api`（2026-06-02）。
- **検証**: cron は新形式キーでも 403（到達）。正本データ路は bcc 5月＝23件/¥1,188,200。admin-api は `/state`・`/receipts/store-options`・`/receipts/sales` が clean 401（ブート正常・ルート健在）。`deno check` の残エラーは既存の DOM型/Supabase総称型/implicit-any ノイズのみ（削除関数への参照ゼロ・構文エラー無し）。
- **残課題（軽微）**: `resolveStorePartitionKeyForRoom` の最終手段（ルーム→店舗キーの履歴推定）がまだ `line_receipt_entries` を参照。これは「どの店舗か」の推定のみで**合計値には影響しない**（引けなければスキップ）。将来必要なら正本ベースへ移行。

### 2026-06-02 レポート集計が「陳腐化した集約表」を見て月合計を取りこぼす不具合を修正（ビストロ サヴァサヴァで顕在化）
- **対象 / 範囲**: receipt-midreport-cron（中間/月末レポート・テスト送信）の集計元。全店共通だが、現状データのある bistrocavacava で顕在化。
- **症状**: 5月レポートの合計が **5/22まで**しか集計されず、5/23以降が欠落。ビストロ サヴァサヴァ5月で **¥838,400（16件）** と表示されるが、実際（売上分析）は **¥1,188,200（23件・5/1〜5/30）**。差 **¥349,800（7件）** が欠落。
- **原因**: レシートの**正本は店舗別テーブル** `line_receipt__<店舗>`（LINE Webhookが保存、**売上分析**が参照）。一方 `line_receipt_entries` は**別の集約表**で、Edge関数のどこからも書き込まれず（トリガーも無し）**5/22以降は更新が止まって陳腐化**していた。ところが**レポート（receipt-midreport-cron）だけがこの陳腐化した `line_receipt_entries` を参照**していたため、5/23以降が抜けた。
- **対策**: `receipt-midreport-cron/functions/_shared/receipt_report_aggregate.ts` の `loadReceiptReportAggregateForStoreByReceiptDate` を、**`store_webhook_tables`（正本レジストリ）から店舗別テーブル名を解決して参照**するよう変更（売上分析と同一ソース）。レジストリのキーは小文字化して突合し、`line_receipt__marugoD` のような大文字混じりの正確なテーブル名で解決。レジストリが引けない場合のみ旧 `line_receipt_entries` へフォールバック。日次上書き（daily overrides）適用は従来どおり。
- **反映**: 関数デプロイ `receipt-midreport-cron`（2026-06-02）。
- **検証**: 新経路を本番データで再現 → bcc 5月 = **23件 / ¥1,188,200 / 営業23日**（売上分析と一致）。旧経路は16件/¥838,400（5/22まで）。`deno check` の警告は既存の implicit-any / 任意引数（index.ts:114 の5引数関数を4引数で呼ぶ既存箇所）であり、本変更とは無関係（Supabaseバンドラでのデプロイは正常）。
- **備考（残課題）**: `line_receipt_entries` は実質未更新の陳腐化表。admin-api の一部（店舗ドロップダウン `fetchReceiptStoreOptions` など）がまだ参照している。売上の正は店舗別テーブルなので、将来的に `line_receipt_entries` 依存箇所の棚卸し/移行を推奨。

### 2026-06-02 レポートのテスト送信に「対象月」を指定できる機能を追加
- **対象 / 範囲**: 管理コンソール（index.html）Webhook設定 →「中間/月末レポートをテスト送信（LINE）」。
- **背景**: 当月はまだレシートデータが少なく、テスト送信しても「送信しませんでした（no_receipt_entries_in_period_for_store）」になりやすい。任意の月で集計プレビューしたい、という要望。
- **対策（フロントのみ）**: テスト送信ボタンの上に `<input type="month" id="testReceiptReportMonth">`（対象月）を追加。値を `year`/`month` に分解して `POST /actions/test-receipt-report` のbodyに付与する。空欄なら従来どおり当月。初期値は「直近の完了月（前月）」を自動セット（データが出やすいため／変更可）。確認ダイアログに「中間レポート（2026年5月1〜15日）」のように対象期間を明示。
- **サーバ側**: 変更不要。`admin-api`（`body.year`/`body.month` を検証して cron へクエリ転送）と `receipt-midreport-cron`（`buildReceiptReportTestSchedule(reportKind, year, month)` で期間算出）は既に year/month 対応済み。
- **反映**: index.html（GitHub Pages）。※push で本番反映。
- **検証**: ローカルプレビューで対象月フィールドが `type=month`・前月(2026-05)初期値で表示、ボタン健在、コンソールエラーなしを確認。期間=中間は当該月1〜15日／月末は当該月1日〜最終日。

### 2026-06-02 全ページのモバイル用メタタグ警告（apple-mobile-web-app-capable 非推奨）を解消
- **対象 / 範囲**: index.html / reservation.html / media.html / analytics.html。
- **症状**: コンソールに `<meta name="apple-mobile-web-app-capable"> is deprecated. Please include <meta name="mobile-web-app-capable">` の警告。
- **対策**: 各ファイルの `apple-mobile-web-app-capable` の隣に標準の `<meta name="mobile-web-app-capable" content="yes">` を併記（Apple版も後方互換で残置）。message-search.html は元々該当タグ無しのため変更なし。
- **反映**: 4ファイル（GitHub Pages）。※push で本番反映。

### 2026-06-02 レポートのテスト送信が「API error: 401」で失敗する不具合を修正
- **対象 / 範囲**: 管理コンソール（index.html）Webhook設定 →「中間/月末レポートをテスト送信（LINE）」ボタン。Edge Function `receipt-midreport-cron`。
- **症状**: テスト送信を押すと `API error: 401`。コンソールにも `…/test-receipt-report → 401 (UNAUTHORIZED_INVALID_JWT_FORMAT)`。一方、定期送信（毎分のpg_cron）や他の連携表示は正常で「トークン認証は通っている」状態。
- **原因**: SupabaseがAPIキーを**新形式**（`sb_publishable_…` / `sb_secret_…`）へ移行済みで、Edgeの環境変数 `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_ANON_KEY` には新形式が入っている（ダイジェスト照合で確認）。`admin-api` はテスト送信時に `Authorization: Bearer <SUPABASE_SERVICE_ROLE_KEY>` を付けて `receipt-midreport-cron` を呼ぶが、**新形式キーはJWTではない**ため、`verify_jwt=true` のゲートウェイが本体到達前に401（Invalid JWT）で弾き、`admin-api` がそれをそのまま返していた。定期送信が無事だったのはpg_cron側が `resolve_edge_cron_auth_token()`（旧形式JWT）を使う別ルートだったため。
- **対策**: `supabase/config.toml` に `[functions.receipt-midreport-cron] verify_jwt = false` を追加（`line-webhook` / `admin-api` / `receipt-sheets-sync-cron` と同じ扱い）。認可は関数側で実施：テスト送信は `X-Receipt-Midreport-Test-Key` の一致必須（不一致は403）、定期送信は重複防止行で冪等。これで新形式キーでも本体まで届く（鍵ローテーション耐性も向上）。
- **反映**: 関数デプロイ `receipt-midreport-cron`（2026-06-02）/ config.toml。
- **検証**: 新形式キー（非JWT）で直接叩いて、デプロイ前=401 → デプロイ後=403（本体到達・ダミー鍵拒否）に変化することを確認。実運用ではブラウザ→admin-api が本物のテストキーを付与するため200（LINEに1通）になる。実送信はLINE通知の副作用があるため画面ボタンで最終確認する。
- **備考（残課題）**: 同じく `verify_jwt=true` の `gmail-alert-cron` は現状pg_cron（旧JWT）経由でのみ呼ばれ稼働中。将来 admin-api 等からHTTPで呼ぶ場合は同じ401になり得るため、その際は同様に `verify_jwt=false` 化を検討。

### 2026-06-01 売上レポートのテスト送信ボタンを復活＋テストキー設定
- **対象 / 範囲**: 管理コンソール（index.html）Webhook設定モーダル「レシート関連」タブの「中間/月末レポートをテスト送信（LINE）」ボタン。
- **症状**: テスト送信ボタンが見つからない（どこにもない）。
- **原因**: ボタン＋注記に `receipt-permission-only-hidden` が付き、**permission モード（連携ルームあり＝稼働中の店舗）で非表示**になっていた。テスト送信は連携ルーム前提なのに、連携ルームのある稼働中店舗で隠れる＝表示条件が逆で実質出てこない状態（[index.html:1131](index.html:1131) のCSS）。
- **対策①（表示）**: テスト送信の注記＋ボタンの `<div>` から `receipt-permission-only-hidden` を除去。稼働中店舗（permission モード）でも表示されるようにした。
- **対策②（キー）**: テスト送信に必要な Edge secret `RECEIPT_MIDREPORT_CRON_TEST_KEY` が**未設定**だったため、`openssl rand -hex 32` でランダム生成して `supabase secrets set` で設定。Supabaseのsecretはプロジェクト共通なので admin-api・receipt-midreport-cron 双方で共有（再デプロイ不要・次回実行から有効）。
- **反映**: コミット（本記録と同コミット）/ index.html（GitHub Pages）＋ Supabase secret。
- **使い方**: Webhook設定 → 店舗の「Webhook設定」→「レシート関連」タブ最下部 →「中間/月末レポートをテスト送信」。連携ルームのある店舗で押すとLINEに1通だけテスト送信（送信ログは残さない・売上分析と同条件で集計）。

### 2026-06-01 予約詳細に「予約受付日（予約が入った日時）」を追加
- **対象 / 範囲**: 予約表（reservation.html）の「予約詳細」モーダル。
- **変更**: 来店日時とは別に、**予約が入った日時（システム記録 = `created_at`、通常は予約メール受信時刻）**を「**予約受付日**」として詳細に表示。
- **実装**: 詳細行に `{ label: '予約受付日', value: formatDateTimeDetailJst(item.created_at) }` を追加（admin-api の予約アイテムは `created_at` を既に返却済みのためサーバ変更なし）。
- **注意**: 今回手動で復旧した予約（内田/眞鍋/木村等）は `created_at` が復旧実行時刻になる。通常フロー（cron毎分）で入る予約は受信時刻＝ほぼ予約された日で正確。
- **反映**: コミット（本記録と同コミット）/ reservation.html（GitHub Pages）。
- **検証**: プレビューで構文OK・コンソールエラーなし。

### 2026-06-01 スマホのハンバーガーメニューを全ページに展開
- **対象 / 範囲**: メディア閲覧（media.html）・予約表（reservation.html）・会話検索（message-search.html）＝ ms-* シェル、売上分析（analytics.html）＝ lsa-* シェル。
- **背景**: ハンバーガー化は index.html（app-v2）だけ対応済みで、他ページはスマホで**サイドバーが全部展開表示**（または横スクロール）されていた。全ページで統一する要望。
- **実装（各ページに同パターン）**:
  - ms-* 3ページ: `#msNavToggle`＋`#msNavBackdrop` 追加、`@media(max-width:880px)` で `.ms-side` をドロワー化（`translateX(-100%)`／`.open`）。z-index はモーダル(40)より下の 30/31/32。
  - lsa-*（analytics）: `#lsaNavToggle`＋`#lsaNavBackdrop`、`@media(max-width:920px)` で `.lsa-side` をドロワー化（横スクロール案を置換）。z-index 680/690/700。
  - 各ページ末尾に開閉JS（ハンバーガー／背景／ナビ項目／Escで開閉）。
- **反映**: コミット（本記録と同コミット）/ 4ファイル（GitHub Pages）。PC表示は不変。
- **検証**: プレビュー mobile(375px) で media.html（ms-*）・analytics.html（lsa-*）のドロワー開閉を確認、reservation/message-search は同一パターン（構造チェック済み）。desktop(1280px) は固定サイドバーのまま。

### 2026-06-01 スマホのヘッダーをハンバーガーメニュー方式に変更
- **対象 / 範囲**: 管理コンソール（index.html）のスマホ表示。
- **変更**: 先の「上部・横スクロールナビ」をやめ、**左上ハンバーガー（☰）→ 左からスライドインするドロワー**方式に変更（要望対応）。
- **実装**:
  - マークアップ: `#navToggle`（ハンバーガー）＋ `#navBackdrop`（背景）を `<body>` 直下に追加。
  - CSS: `.app-v2-navtoggle`/`.app-v2-nav-backdrop`（PCは `display:none`）。`@media(max-width:920px)` でサイドバー `.app-v2-tabs-wrap` を `transform:translateX(-100%)` のドロワー化、`.open` でスライドイン。z-index はヘッダー(500)/サイドバー(600)より上・モーダル(1000+)より下（680/690/700）。
  - JS: ハンバーガーで開閉、背景クリック／ナビ項目クリック／Esc で閉じる（末尾 `<script>`）。
- **反映**: コミット（本記録と同コミット）/ index.html（GitHub Pages）。**デスクトップ表示は不変**。
- **検証**: プレビュー mobile(375px) でハンバーガー→ドロワー開閉OK・背景クリックで閉じる・コンソールエラーなし。desktop(1280px) は従来の固定サイドバーのまま（ハンバーガー非表示）。

### 2026-06-01 「保存中」が消えない不具合を修正＋Gmail予約通知が保存で消える根本対策
- **対象 / 範囲**: 管理コンソール（index.html）のWebhook/ルーム設定の保存フロー。全店舗。
- **症状①**: 設定を保存すると「保存中…設定を保存しています」オーバーレイが**成功しても閉じず固まる**。
- **原因①**: `saveRoomConfigBtn` のクリックハンドラが、`hideSyncOverlay` を **catch（失敗時）でしか呼んでおらず、成功時に閉じていなかった**（[index.html:13618](index.html:13618) 付近）。他の保存ハンドラは両方で閉じている。
- **対策①**: `hideSyncOverlay(overlayToken)` を `finally` に移動。成功・失敗どちらでも必ず閉じる。
- **症状②（根本対策）**: Gmail予約通知（`gmail_reservation_alert_enabled`）が保存のたびにOFFに戻る（前エントリ参照）。
- **原因②**: `resolveCalendarGmailForRoomSave` が、ルーム個別オーバーライド無しのとき店舗一括（既定OFF）でGmailフラグを上書き。
- **対策②（全店向け）**: 個別オーバーライド無しでも、**既にONのルームのGmail予約通知は保存で維持**する no-downgrade ガードを追加（[index.html:7579](index.html:7579)）。OFFにするには「このルームだけ一括と違う設定にする」をONにして明示的に切る。
- **反映**: コミット（本記録と同コミット）/ index.html（GitHub Pages）。サーバ変更なし。
- **検証**: プレビューで構文OK・コンソールエラーなし。※実保存の往復はローカル→本番のCORSのため未検証。

### 2026-06-01 メール予約の取りこぼし（既読メール）を解消＋欠落分を復旧
- **対象 / 範囲**: gmail-alert-cron（メール予約 → LINE通知 ＋ 予約表）。ビストロ サヴァサヴァで顕在化。
- **症状**: 5/28以降のメール予約が「予約表（サイトのカレンダー）」に登録されない。通知も来ない。
- **原因（実DB照会で確定）**:
  1. cronの検索条件が **`is:unread`（未読のみ）／1回最大5件**。店舗が予約メールを**開封(既読)すると `is:unread` で除外**され、既読の予約は永久に取りこぼす（既読も拾う緩い検索は「未読0件」のときしか発動しない）。内田6/3・眞鍋5/30・木村6/8(一休)はこれで未処理だった。
  2. さらに `gmail_reservation_alert_enabled`（Gmail予約通知）が**全ルームでOFF**になっており、cronが連携先無しで即終了していた（一度ONにしても後で外れていた）。
- **対策**:
  - **コード（gmail-alert-cron）**: 既定クエリから `is:unread` を除去（既読・未読の両方を対象。二重通知は `gmail_reservation_alert_logs` の通知済みIDで防止）。env で query 指定があっても `is:unread`/`label:unread` を強制除去。`DEFAULT_GMAIL_ALERT_MAX_MESSAGES` を 5→20 に（7日分の取りこぼし対策）。→ 再デプロイ。
  - **データ復旧**: ビストロの主ルーム「Cava Cava」の Gmail予約通知を ON（room_summary_settings を直接更新）→ cron手動実行。
- **結果**: 欠落していた 内田6/3・小田6/6・眞鍋5/30・木村6/8(一休)＋高石6/8・嶋田5/30・杉田5/28 を `tabelog/ikyu_reservation_visit_events` に記録（予約表に反映）。
- **反映**: コミット（本記録と同コミット）/ gmail-alert-cron 再デプロイ済み。
- **「OFFに戻る」原因と対策（追記）**: `gmail_reservation_alert_enabled` は「カレンダー/予約」の一括＋ルーム個別オーバーライドで管理される。ルームの `calendar_gmail_settings_override` がOFFだと、**保存のたびに店舗一括（既定OFF）で上書き**される（index.html `resolveCalendarGmailForRoomSave`）。そのため個別にONにしても次の保存で戻っていた。対策として **Cava Cava のドラフト（console_settings `line_room_calendar_gmail_settings__<roomId>`）に `calendar_gmail_settings_override:true`＋`gmail_reservation_alert_enabled:true` を設定**し、保存しても維持されるようにした（room_summary_settings 側も true）。他店舗でも同様に「このルームだけ一括と違う設定にする」ON で運用すれば外れない。根本対策（保存でリセットしない）はコード改修案として保留。
- **メモ（運用知識）**: 本番DBは MCP `execute_sql`/`get_logs` が権限拒否。psqlも未インストール、CLIにSQLクエリ無し。読取/書込が必要な時は **REST API（`/rest/v1`）＋ service_role キー**（`supabase projects api-keys` で取得、キーは出力しない）で実施可能。

### 2026-06-01 スマホ（iPhone）でヘッダー/ナビが縦割れする表示崩れを修正
- **対象 / 範囲**: 管理コンソール（index.html）の app-v2 シェル（サイドバー＋トップバー）。スマホ表示。
- **症状**: iPhone等の狭い画面で、サイドバーのナビ（特に「ページ」群: メディア閲覧/会話検索/予約表/売上分析）が**1文字ずつ縦に割れて表示**され、ヘッダーが崩れていた。
- **原因**: `@media (max-width: 920px)` でサイドバーを横並びにしていたが、**`.app-v2-pages`（ページ群）と `.app-v2-pagelink` が未対応**で、縦並び（column）のまま幅が潰れて文字が縦割れしていた（`white-space:nowrap` も無し）。
- **対策（index.html・CSSのみ）**: 920px以下のメディアクエリに以下を追加。
  - `.app-v2-tabs-wrap > * { flex:none }`（各要素を縮めない→横スクロールで見せる）
  - `.app-v2-pages` を横並び（row）化、`.app-v2-pages-label`（メニュー/ページ見出し）は非表示
  - `.app-v2-tab, .app-v2-pagelink { white-space:nowrap; flex:none }`、ページリンクの外部リンク矢印・ブランド副題は省略
  - 結果: スマホでは上部の**横スクロール式ナビ**になり、縦割れ解消。
- **反映**: コミット（本記録と同コミット）/ index.html（GitHub Pages）。サーバ変更なし。デスクトップ（>920px）は未変更（メディアクエリ内のみ）。
- **検証**: プレビューを mobile(375px)にして確認。ナビ各項目が横並びのピル表示（メディア閲覧 123×38px等、幅>高さ＝縦割れ無し）、横スクロール動作OK、コンソールエラーなし。※他ページ（media/予約表/会話検索/売上分析）は別シェルのため未対応（必要なら別途）。

### 2026-06-01 メディア閲覧の店舗セレクタに保存件数を表示
- **対象 / 範囲**: メディア閲覧（media.html）＋ admin-api。全店舗。
- **要望**: 店舗セレクタの各店名の横に、保存メディア件数を「店名（N件）」で表示したい。
- **実装**:
  - **admin-api**: メディア一覧APIのレスポンスに `store_media_counts`（店舗キー→件数、全種別・絞り込み非依存）を追加（新ヘルパ `fetchMediaCountByStore`、`line_message_media` を store_partition_key で集計）。
  - **media.html**: `setMediaStoreFilterOptions` を件数対応にし、`loadMedia` でAPIの `store_media_counts` を受け取りセレクタを再構築。「マルゴ オット（3件）」「全店舗（19件）」のように表示（全店舗は全体総数 all_file_count）。件数取得前は従来どおり括弧なし。
- **反映**: コミット（本記録と同コミット）/ admin-api 再デプロイ＋ media.html（GitHub Pages）。
- **検証**: media.html はコンソールエラーなし（構文OK）、admin-api は `deno check` 新規エラーなし。※ローカルプレビューは本番データを読めないため、実件数表示は本番で確認。

### 2026-06-01 メディア閲覧の「投稿者: 記録なし」を修正（送信者名を解決）
- **対象 / 範囲**: メディア閲覧（media.html）。全店舗のルーム受信メディア。
- **症状**: メディア一覧の「投稿者」が常に「（記録なし）」。
- **原因**: 受信メディア保存時、`sender_display_name` を**ハードコードで null** にしていた（[line_media_store.ts](supabase/functions/_shared/line_media_store.ts)）。送信者の `user_id` は保存しているが名前を解決していない。さらに media.html は `sender_display_name` のみ表示し user_id へフォールバックしないため、常に「記録なし」になっていた。
- **対策**:
  - **保存時（今後分）**: `saveRoomMediaToLibrary` で `fetchLineDisplayNameByUserId(user_id, room_id, token)`（LINE API: グループ/ルームのメンバー名→プロフィール）を呼び、`sender_display_name` に保存。失敗時は null のまま（graceful）。→ line-webhook 再デプロイ。
  - **表示時（既存分の補完）**: admin-api のメディア一覧で、`sender_display_name` が空かつ `user_id` があれば `line_user_permissions.display_name` から補完（新ヘルパ `fetchSenderNameMapForUserIds`）。→ admin-api 再デプロイ。
  - ※ `user_id` 未記録の古い行や、`line_user_permissions` に名前が無い送信者は補完不可（その場合は今後分から表示）。
- **反映**: コミット（本記録と同コミット）/ line-webhook・admin-api 再デプロイ（本番適用済み）。サーバのみ（フロント変更なし）。
- **検証**: `deno check` 新規エラーなし（既存ノイズのみ）。実地は新規にメディアを送信→投稿者名が表示されること、既存メディアは送信者が `line_user_permissions` 登録済みなら表示されることを確認（運用）。

### 2026-06-01 ルーム「個別設定」で“そのルーム自身”の権限トグルを読み込めるよう修正
- **対象 / 範囲**: 管理コンソール（index.html）の「権限・機能設定」モーダル。全店舗の連携ルーム個別設定に影響。
- **症状**: 連携ルームが複数ある店舗（例: マルゴオット＝3ルーム）で、各ルームのトグル（レシート解析結果送信・中間/月末ON-OFF・その他権限など）を**個別に設定できない**。送信時刻（中間/月末の時間・日）は対象ルームごとに変えられるのに、トグルは個別にできない。
- **原因**: このモーダルは2スコープ混在。①送信時刻・カレンダー/予約＝ルーム個別（対象ルームで切替）②トグル類＝店舗一括。さらに各ルームの「個別設定」ボタン（順位:ルーム個別）で開いても、トグルが `aggregateWebhookConfig()`＝**連携先頭ルーム(rows[0])の値**で表示され（`getRoomConfigStateFromRow(rows[0])`）、対象ルーム自身の保存値が出ないため「個別設定できない／保存が効かない」ように見えていた。
- **対策（index.html）**:
  - `applyRoomConfigPermissionToggles(config)` を新設（トグル反映処理を関数化）。
  - `openWebhookConfigModal`: ルーム個別モード（個別設定ボタン）では `mergeGlobalIntoConfig(getRoomConfigStateFromRow(activeRoomConfigRow))`＝**そのルーム自身**の保存値を表示するよう変更（店舗一括モードは従来どおり）。
  - `onRoomConfigReceiptScheduleRoomChange`: 個別モードで対象ルームを切替えたら、トグルもそのルームの値に読み込み直す（古い値を別ルームに保存する事故を防止）＋メタ表示更新。
  - 保存は従来の `saveIndividualRoomConfigFromModal`（そのルームだけに保存・順位:ルーム個別）を使用。店舗一括保存はルーム個別を上書きしない既存仕様のまま。
- **使い方**: 全連携ルーム共通に変えたい → 店舗のWebhook設定で保存。特定ルームだけ変えたい → 連携ルーム一覧の各ルームの「**個別設定**」ボタンから保存（順位:ルーム個別）。
- **反映**: コミット（本記録と同コミット）/ index.html（GitHub Pages）。サーバ側の変更なし。
- **検証**: プレビューでページが正常描画・コンソールエラーなし（構文/初期化OK）。店舗一括モーダルの描画ロジックは関数抽出のみで挙動不変。※実データのルーム個別の往復はユーザー環境での実機確認が必要。

### 2026-06-01 マルゴオット: 期間集計レポートの返信ゲートを是正＋無反応(マーカー欠落)を修正
- **対象店舗 / 範囲**: マルゴオット（marugootto）。直前の「期間集計レポートはリッチ返信」の続き。
- **症状①（無反応）**: 期間集計レポート画像を送っても無反応だった。
- **原因①**: マイグレーション（020000/030000）は本番適用済みで店舗プロンプトは効いていたが、ビジョンモデルが `kind=general` までは従う一方、`summary` に判定マーカー「期間集計」を入れず（言い換え/省略）、webhook の期間集計スキップ分岐が**発火しなかった**。結果、非レシート画像扱いに落ち、当店は「画像解析結果を送信（非レシート）」OFF のため返信も抑止＝**無反応**となっていた。
- **対策①（プロンプト強化）**（migration `20260601040000`）: marugootto プロンプトを、**summary の先頭に必ず固定文字列「期間集計レポート」を置かせ、言い換え・省略・翻訳を禁止**するよう厳格化（`kind` も明示的に `general` を強制、`receipt=null`）。検知正規表現 `/期間集計|.../` が確実に拾う。
- **症状②（ゲート要件）**: 直前の追補で通知を `rawReplyToken` の**無条件返信**にしていたが、要望は「**『レシートの解析結果を送信』OFF では返信しない**」。
- **対策②（返信ゲート是正）**: line-webhook の期間集計通知を `rawReplyToken`→**`receiptReplyToken`** に戻した。これにより「レシートの解析結果を送信」**OFF→送らない**／**ON→送る**。AI返信完全無し(hard mute)でも、レシート関連返信は対象外なので**送る**（既存仕様どおり）。売上は引き続き登録しない（`saved:false`）。
- **反映**: コミット（本記録と同コミット）/ migration `20260601040000` / line-webhook 再デプロイ（本番適用済み）。
- **検証**: `deno check` 新規エラーなし（既存の `_shared/*` 等のノイズのみ）。実地はマルゴオットへ期間集計レポートを送り、(1)売上に登録されない (2)「レシートの解析結果を送信」ONなら「登録していません」のリッチ返信が来る（AI返信完全無しでも）／OFFなら返信が来ない、ことを確認（運用）。通常レシートは従来どおり。

### 2026-06-01 マルゴオット: 期間集計レポートは「登録しません」とリッチ返信（AI返信完全無しでも）
- **対象店舗 / 範囲**: マルゴオット（marugootto）。直前の「期間集計レポート除外」の挙動変更。
- **変更**: 直前実装は期間集計（GP/期間）レポートを**無反応スキップ**にしていたが、要望により「これは日々の売上レシートではないため**登録していません**」と**リッチテキスト(Flex)で返信**するように変更（売上には引き続き登録しない）。
- **重要**: この返信は**レシート解析返信の経路（`receiptReplyToken`）**で送るため、**AI返信完全無し（hard mute）でも送信**される（「レシートの解析結果を送信」がONのとき。レシート関連返信は hard mute の対象外という既存仕様に合わせた）。
- **実装**:
  - **line-webhook**: `buildReceiptNonSalesNoticeFlex()` を追加し、期間集計マーカー検知時に Flex 通知を `receiptReplyToken` で返信。売上登録はしない（`saved:false`）。
  - **店舗プロンプト**（migration `20260601030000`）: marugootto の summary を「このレシートは日々の売上ではないため登録していません（期間集計レポート）」に更新（検知マーカー「期間集計」は維持。Flex本文にも表示）。
- **反映**: コミット `6581a79` / migration `20260601030000` / line-webhook 再デプロイ（本番適用済み）。
- **検証**: `deno check` 新規エラーなし。実地はマルゴオットへ期間集計レポートを送り、(1)売上に登録されない (2)「登録していません」のリッチ返信が来る（AI返信完全無しでも）ことを確認（運用）。通常レシートは従来どおり。
- **追補（同日・無反応の修正）**: 実機で無反応だったため、通知を `receiptReplyToken`→`rawReplyToken` に変更して**無条件返信**（「レシートの解析結果を送信」OFFでも必ず送る／売上は登録しない）。検知マーカーも `GP（グループ）`/`［期間］`/`日付範囲` 等のバリアントを追加。コミット `581d43c` / line-webhook 再デプロイ。※なお無反応の原因が「モデルが期間集計と判定していない（売上として登録された）」場合は別途プロンプト強化が必要。

### 2026-06-01 マルゴオット: 期間集計(GP/期間)レポートを売上記録・返信の対象外に
- **対象店舗 / 範囲**: マルゴオット（marugootto）。
- **背景**: マルゴオットは通常の売上レシートに加えて、POSの「期間集計レポート（GP（グループ）／期間。日付範囲の集計・構成比・グループ別点数/金額）」画像が送られることがある。これは売上レシートではないのに、売上として記録・返信されてしまう恐れがあった。
- **対策（プロンプト＋webhook）**:
  - **店舗プロンプト**（migration `20260601020000`）: 画像に「期間」「日付範囲」「GP（グループ）」を含む集計レポートは `kind=general`・`receipt=null` とし、`summary` を「期間集計レポート」で始めるよう指示。通常の1会計レシートは従来どおり解析。
  - **line-webhook**: 画像解析の `summary` に期間集計マーカー（`期間集計`／`日付範囲`／`GP（グループ）`）があれば、**売上に加算せず・返信もせず**早期 return（受信メディアの保存自体は従来どおり）。マーカー方式のため、誤って受信メディア保存以外の副作用は無し。
- **反映**: コミット `5cc8164` / migration `20260601020000` / line-webhook 再デプロイ（本番適用済み）。
- **検証**: `deno check` 新規エラーなし。実地はマルゴオットのルームへ当該「期間集計レポート」を送り、売上に加算されず・返信が来ないことを確認（運用）。通常レシートは従来どおり記録されることも併せて確認。

### 2026-06-01 管理コンソールの機能/権限設定をDBで端末間共有（localStorage廃止）
- **対象店舗 / 範囲**: 管理コンソール全体。全体・店舗の既定値、ルーム優先順位、ルーム個別の下書き等。
- **背景/症状**: これらの設定がブラウザ localStorage に保存されており、**端末ごとに異なり共有されなかった**（端末Aで変更しても端末Bに反映されない）。
- **対策**:
  - **DB**: `line_admin_console_settings(setting_key text PK, setting_value text, updated_at)` を新設（migration `20260601010000`）。
  - **admin-api**: `/state` レスポンスに `console_settings`（key→値）を同梱。`PUT /settings/console` で `{settings:{key:value}}` を一括 upsert。
  - **フロント(index.html)**: メモリキャッシュ `consoleSettings` ＋ `csGet/csSet/csRemove` を導入。`loadBooleanSetting`/`loadIntegerSetting` 等の基礎関数、全体権限・店舗テンプレ（`WEBHOOK_DEFAULT_CONFIG`/`WEBHOOK_STORE_BULK_CALENDAR_GMAIL`）、ルーム優先順位、ルーム個別カレンダー下書き、明日予定の時刻既定など、**機能/権限設定の localStorage 読み書きをすべて DB バックへ差し替え**。`/state` から seed、変更は debounce で `PUT /settings/console`。**初回のみ既存 localStorage を一度だけ DB へ移行**（運用中の設定を失わない）。保存直後の再読込で未保存分が消えないよう seed は未送信分を温存。
  - **端末ローカル維持**: ログイントークン（セキュリティ）、テーマ（個人の好み）、Webhookログ絞り込み・「その他設定の開閉」等の一時UI（`CONSOLE_SETTINGS_LOCAL_ONLY`）。
- **反映**: コミット `f83f93b` / migration `20260601010000` / admin-api 再デプロイ / GitHub Pages push（本番適用済み）。
- **検証**: プレビューで csGet/csSet・dirty追跡・ローカル維持判定が正しく動作、コンソールエラーなし。実機は端末Aで設定変更→別端末Bでハードリロードし共有反映を確認（運用）。

### 2026-06-01 全体モードのカレンダー/予約設定が保存→リロードで元に戻る
- **対象店舗 / 範囲**: 管理コンソール（index.html）「権限・機能設定」→ 全体（全店舗・全ルーム）→「カレンダー/予約」タブ。
- **症状**: 全体でカレンダー/予約のトグル（明日予定配信・カレンダー自動登録・Gmail予約通知 等）を変えて保存しても、スーパーリロードすると元に戻る。
- **原因**: 全体保存 `saveGlobalPermissionFromModal` は、カレンダー値を**各ルームのDB行には反映**する一方、全体モーダルが**表示時に読む既定キー**（`getBulkCalendarGmailSettings` が参照する `NEW_ROOM_DEFAULT_*` localStorage）へは保存していなかった。そのため再表示で既定値（変更前）に戻っていた。※レシート関連／その他権限のトグルは `GLOBAL_PERMISSION_CONFIG_KEY` に保存され表示も往復するため影響なし。
- **対策**: 全体保存時に、インラインのカレンダー/予約バルク（6トグル＋明日予定の時刻・「予定がある日のみ」・最大件数）を**既定キーへも保存**する `persistGlobalBulkCalendarGmailFromInline()` を追加。これで全体モーダルの再表示でも値が往復し、新規ルームの既定にも反映される。
- **反映**: コミット `ffe66bd` / GitHub Pages push（フロントのみ・再デプロイ不要）。
- **検証**: index.html インラインJS構文OK。実機はハードリロード後、全体→カレンダー/予約で変更→保存→スーパーリロードで値が保持されることを確認。
- **補足**: 全体の既定値はこれまで通り localStorage（端末ごと）保持で、実際の各ルーム設定はDBへ反映される設計（既存の「新規ルームの既定値」と同じ仕組み）。

### 2026-06-01 全体権限モーダルのカレンダー/予約タブが「連携ルームなし」で編集不可
- **対象店舗 / 範囲**: 管理コンソール（index.html）の「権限・機能設定」→ 全体（全店舗・全ルーム）モード →「カレンダー/予約」タブ。
- **症状**: 全体設定なのに「連携ルームがありません」と表示され、明日予定リマインド・カレンダー自動登録・Gmail予約通知などの一括入力が非表示＋無効化されて**編集できない**。
- **原因**: `syncRoomConfigCalendarTabLayout` / `syncRoomConfigCalendarGmailPermFields` が、対象ルーム数 `linkedCount`（＝アクティブ店舗に連携されたルーム数）で表示可否を判定していた。全体モードでは疑似店舗 `__global__` に紐づくルームが 0 件のため `linkedCount===0` となり、「連携ルームなし」扱いになっていた。
- **対策**: `isGlobal`（`activeWebhookConfigStoreKey === __global__`）のときは `linkedCount` に依存せず、一括入力を**表示・有効化**し、「連携ルームがありません」ヒントを出さないよう修正（タイトルも「全体一括（全店舗・全ルームへ保存時に反映）」に）。保存は従来どおり全ルームへ反映（`resolveCalendarGmailForRoomSave` 経由のインライン一括値）。
- **反映**: コミット `13c0bee` / GitHub Pages push（フロントのみ・再デプロイ不要）。
- **検証**: index.html インラインJS構文OK。実機はハードリロード後、全体→カレンダー/予約タブで一括設定が編集可能・「連携ルームなし」が出ないことを確認。

### 2026-06-01 レポート送信cronをルーム個別の送信時刻（日・時・分）に対応＋毎分起動
- **対象店舗 / 範囲**: 全店舗。中間／月末（月間）レポートの自動送信（`receipt-midreport-cron`）。
- **背景**: 送信時刻がコード固定（中間=16日／月末=翌月1日、ともに10:00 JST）で、UI＋DBに保存したルーム個別時刻が反映されていなかった。
- **重要な発見**: `receipt-midreport-cron` は**このリポジトリ外**で管理されていた（依存共有ファイルが repo と相違6・欠落3）。そこで CLI（Docker起動→`supabase functions download`）で**本番コードを取得して正本化**し、依存は**この関数専用に `./functions/_shared/` へ固定**（他機能の共有コードには影響させない）。
- **対応**:
  - 送信時刻判定を「固定」→「**ルームごとの `receipt_schedule_override` ＋ 中間/月末の `day`・`hour`・`minute` を優先、無ければ既定（中間16日／月末翌月1日, 10:00）**」に変更。期間（中間=当月1〜15日／月末=前月まるごと）と二重送信防止は従来同等。
  - **分単位**を効かせるため `pg_cron` で**毎分起動**（migration `20260601000000`、既存の `invoke_*`＋`net.http_post` パターン）。
  - **二重送信対策**: 複数スケジューラ併存でも安全なよう、**送信前に重複防止行（`line_receipt_mid_reports` の一意制約）を確保**してから push し、送信失敗時は予約行をロールバックする実装に変更。
- **反映**: コミット `b3dd4dc` / migration `20260601000000` / `receipt-midreport-cron` 再デプロイ（本番適用済み）。
- **検証/確認事項**: `deno check` は既存ノイズ（未型付けJSの implicit-any 等）のみ。**実送信は運用確認**：①管理画面「月末レポートをテスト送信」で1ルーム確認、②本日(6/1)10:00の月末送信、③個別時刻を数分後に設定して発火確認。`resolve_edge_cron_auth_token()` は既存 receipt-sheets-sync と同じ仕組みのため動作前提。
- **注意（保守）**: この関数は `./functions/_shared/` に依存を固定したため、`supabase/functions/_shared/` を編集してもこの cron には反映されない（意図的。挙動を変えないための措置）。将来共有コードを統一する場合は別途要対応。

### 2026-05-31 レポート送信時刻をルーム単位でDB永続化（localStorage廃止）
- **対象店舗 / 範囲**: 全店舗。レポート（中間・月末）の送信時刻のルーム個別設定。
- **症状/背景**: 「このルームで送信時刻を個別設定する」の値（中間/月末の 日・時・分）が**ブラウザ localStorage にしか保存されず**、サーバ（cron）や他端末から取り出せなかった。
- **対策**:
  - **DB**: `room_summary_settings` に `receipt_schedule_override`（bool）＋ `receipt_midreport_day/hour/minute`・`receipt_monthend_day/hour/minute`（int, NULL=既定継承）を追加（migration `20260531210000`）。
  - **admin-api**: `buildRoomSettingsPayload` で7項目を検証（override=boolean、各時刻は範囲内のint or null）し、PUT `/settings/rooms` の upsert に追加。`/state` は `select *` で返却。
  - **フロント(index.html)**: 送信時刻を**行 dataset（DB由来）で保持**し、保存時に PUT、読込は DB から。`loadRoomReceiptSchedule`/`saveRoomReceiptSchedule` を localStorage → 行 dataset に変更（個別設定の localStorage 保存・読込を撤去。旧キーの掃除のみ残置）。一括フラグ適用で他ルームの時刻を消さないようガード。
- **反映**: コミット `e40854b` / migration `20260531210000` / admin-api 再デプロイ / GitHub Pages push。
- **⚠️ 残課題（重要）**: 実際にこの時刻でレポートを送る cron（**`receipt-midreport-cron`、本リポジトリ外**）が新カラムを読むよう更新するまでは、送信時刻はDBに保持・取得できるものの、送出自体は既定スケジュール（翌月1日 等）で行われる。サーバ送出を個別時刻に合わせるには当該 cron の更新が必要。
- **検証**: `deno check` 新規エラーなし。index.html インラインJS構文OK・読み込みコンソールエラーなし。実DB往復（保存→リロードで保持）はトークンが要るため本番反映後に確認。

### 2026-05-31 全ページのサイドバーを共通化（メニュー＋ページ＋売上シート）
- **対象店舗 / 範囲**: 管理コンソール全ページ（index / analytics / media / reservation / message-search）。全店舗共通。
- **症状**: ページによってサイドバーの並び・項目・グループが違っていた。index は「メニュー」ラベル無し・売上シートあり、analytics は「運用／ページ／監査」の独自グループでページリンク欠落、ms-*（メディア/予約表/会話検索）は売上シート無し。
- **対策**: 全ページで「**メニュー**: 接続設定／利用状況／Webhook設定／ログ／ユーザー権限」＋「**ページ**: メディア閲覧／会話検索／予約表／売上分析／売上シート」に統一。
  - index.html: タブ群の前に「メニュー」ラベルを追加（タブ機能は維持）。
  - analytics.html: `lsa-nav` を運用/ページ/監査→メニュー/ページへ再構成し、ページリンク（メディア閲覧/会話検索/予約表/売上シート）とアイコンを追加。LINE 導線時の非表示CSSは維持。
  - media/reservation/message-search: ページグループに売上シートのナビを追加し、`/receipts/sheets-pilot-link` を取得して動的にリンク設定（`refreshSalesSheetNavLink`）。`.ms-navlink[hidden]` の非表示CSSも追加。
  - 売上シートは「パイロット有効＋ログイン＋スプレッドシート設定済み」のときのみ表示の動的リンクで、全ページ共通の挙動。未ログイン/未設定時は非表示。
- **反映**: コミット `9ec9fa7` / GitHub Pages push。
- **検証**: 5ページのサイドバーをプレビューで確認し、ラベル（メニュー/ページ）・項目・順序・アクティブ表示が一致。売上シートは未ログイン時に各ページで非表示。インラインJS構文チェックOK、コンソールエラーなし。

### 2026-05-31 バルぺロタの精算レシートで「客数」が抽出できない＋客単価を確実に算出
- **対象店舗 / 範囲**: バルぺロタ（barpelota）。精算（レジ締め・日計）レシート。
- **症状**: 「客数」（例: 43）が解析されず、客単価も入らない。
- **原因**: 精算レシートは数値項目が多く（総取引数・通常取引数・各取引数・販売点数・返品点数…）、軽量ビジョンモデルが「客数」を取り違え/見落とす。客数が取れないと客単価（コードは `合計 ÷ 客数` で自動計算）も発火しない。初期プロンプトは「客数=客数」程度で混同対策が弱かった。
- **対策**: バルぺロタの補足プロンプトを精算フォーマット専用に強化（項目対応を明示＋「総取引数/通常取引数/各取引数/販売点数/返品点数は客数に入れない」と混同禁止を明記）。**客単価はコード側（[receipt_parse.ts:306-312](supabase/functions/_shared/receipt_parse.ts:306)）が `合計 ÷ 客数` で決定的に算出**するため、プロンプトでは `unit_price` を出力させず（=コード計算を確実に発火）、客数の確実な抽出に集中させた。割り算をAIにさせないので算出は「間違いなく実行」される。客単価例: 186,350 ÷ 43 ≒ ¥4,334。
- **反映**: コミット `a9fd876` / migration `20260531200000_barpelota_receipt_prompt_kyakusu.sql`（`supabase db push --linked` で本番適用済み）。関数の再デプロイ不要（プロンプトは解析時にDBから都度読込）。
- **検証**: マイグレーション適用成功。実地検証は当該精算レシートを再送し、客数=43・客単価が入ることを確認（運用）。

### 2026-05-31 メディア閲覧: Webhook保存メディアが「件数だけ出て一覧に表示されない」不具合
- **対象店舗 / 範囲**: メディア閲覧（admin-api `/media`）。Webhookで保存された受信メディア全般（バルペロタで発覚）。
- **症状**: メディア保存ONでレシート画像を送っても一覧に出ない。「表示: 1-0 / 全2件」「該当するメディアがありません」と、**件数（全2件・799.2KB）はカウントされるのに一覧は0件**という矛盾表示。
- **原因**: `line-webhook` が受信メディアを保存する際、`line_message_media.message_id` は **null**（`line_messages` 行に紐付かないため）。一方 admin-api の `normalizeMediaListRow()` が `message_id` を**必須**として扱い、null の行を一覧から除外していた（件数・使用量は別集計なので2件と出るが、一覧アイテムは0件にフィルタされる）。＝保存は正常、**表示側の取りこぼし**。
- **対策**: `normalizeMediaListRow()` の必須チェックから `message_id` を外し（必須は `line_message_id` ＋ストレージ情報）、型を `string | null` に変更。`message_id: messageId || null` を返す。`fetchMediaContextMap()` も null を許容（anchor抽出を文字列のみに絞り、null時はルーム＋時刻で前後文脈を取得）。データ移行は不要（行は既に正しく保存済み）。
- **反映**: コミット `36cc2ee` / 関数デプロイ `admin-api`。
- **検証**: `deno check` で新規型エラーなし（既存ノイズのみ）。デプロイ後、メディア閲覧の店舗フィルタで保存済みレシート画像が一覧表示されることを確認予定。

### 2026-05-31 サイドバーの「売上シート(H)」重複リンクを削除
- **対象店舗 / 範囲**: 管理コンソール（index.html）サイドバー「ページ」グループ。全店舗共通。
- **症状**: 「ページ」に **売上シート** と **売上シート(H)** の2つが並び、別シートのように見える。
- **原因**: 2つは別シートではなく**同一スプレッドシートURLを指す重複リンク**。旧レイアウトで「アクション用」と「ヘッダー用(H = Header)」の2箇所に置いていたアンカー（`receiptSheetsPilotActionLink` / `receiptSheetsPilotHeaderLink`）が、サイドバー化の際に両方とも「ページ」に並んでしまった。`setReceiptSheetsPilotLinkVisible()` が両方に同じ `spreadsheet_url` を設定していた。
- **対策**: `receiptSheetsPilotHeaderLink` の要素（index.html）と JS 配列の参照を削除し、「売上シート」1つに統一（リンク先は不変）。
- **反映**: コミット `9baa514` / GitHub Pages push。
- **検証**: インラインJS構文チェックOK。プレビューで「ページ」が メディア閲覧／会話検索／予約表／売上分析／売上シート の5項目になり 売上シートが1つだけ・HeaderLink要素が消滅・コンソールエラーなしを確認。

### 2026-05-31 会話検索ページを共有コンソールシェルへ移行・刷新（design_handoff_conversation_search）
- **対象店舗 / 範囲**: 会話検索（message-search.html）。全店舗共通の管理コンソール。
- **症状**: 会話検索だけが旧レイアウト（暗いページ上部に小さな検索パネルが浮く＋冗長な接続フォーム）のまま取り残され、メディア閲覧・予約表・各管理ページの共有シェル（左サイドバー＋トップバー）に未統一だった。また `admin-auth.js`（存在しないファイル）を読み込んでおり、共有セッションを正しく利用できていなかった。
- **対策**:
  - メディア閲覧／予約表と同じ `ms-*` シェル（264px サイドバー＋トップバー＋1180px センターカラム）へ移行。サイドバー「ページ」グループで **会話検索** をアクティブ表示。
  - 冗長な接続フォーム（未接続ピル／管理画面へ／トークン入力／保存して接続）を撤去し、接続状態はトップバーのピル＋サイドバー下部に集約。JS 互換のためトークン入力・保存ボタンは非表示で保持。
  - 検索カードをハンドオフ通りに再構成: セグメント型タブ（ルーム会話／全会話（横断）／ルーム予定）＋検索行（ルーム選択｜虫めがね付き検索ボックス｜検索ボタン）＋ヒント。検索結果カードに空状態（破線ボックス＋「キーワードを入力して検索してください」）を追加。
  - 全会話（横断）モードでは従来の「ルーム選択を非表示」から **「無効化＋減光（is-disabled）」** に変更し、グリッド3列レイアウトを維持。
  - 読み込みスクリプトを `admin-auth.js` → **`auth-session.js` + `site-cache.js`** に修正（他ページと同じ共有セッション／キャッシュ機構へ統一）。テーマは共有キー `line_reservation_calendar_theme` で永続化。
  - 検索ロジック・DOM ID・ルーム選択アコーディオン（同名グルーピング→店舗別2段目）は全て温存。LINE 経由アクセスの売上分析リダイレクトガードも維持。
- **反映**: コミット `36d743a` / GitHub Pages push。
- **検証**: インラインJS構文チェック（vm.Script）OK。プレビューでシェル描画（ダーク／ライト両テーマ）・タブ切替でヒント文＆ルーム選択の無効化/減光・テーマトグルの永続化・ルーム選択ドロップダウンの開閉と配置を確認。コンソールエラーなし。

### 2026-05-31 メディア閲覧の容量表示が旧仕様（2GB）のまま → 1ルーム20MBの実態に修正
- **対象店舗 / 範囲**: メディア閲覧。
- **症状**: 「メディア容量使用率 0 B / 2.00 GB」と表示。現行仕様は「1ルーム20MB（超過分は古い順に自動削除）」で、2GB はグローバル上限時代の名残＝誤解を招く表示。
- **対策**: グローバル 2GB ゲージを廃止し、「**保存ルール: 1ルームあたり最大20MB（超過分は古い順に自動削除）**」の注記に置換。`renderUsageBar` は表示要素が無ければ no-op。併せて店舗ドロップダウンの既定表示も「全ルーム」→「全店舗」に。
- **反映**: コミット `4aabba8` / GitHub Pages push。
- **検証**: 容量ボックスが20MB注記になり2GB表示が消えること、`renderUsageBar` がエラーなく no-op になることを確認。

### 2026-05-31 メディア閲覧の絞り込みを「店舗単位（22店舗）」に変更
- **対象店舗 / 範囲**: メディア閲覧。
- **症状**: 絞り込みリストが LINE ルーム基準で、個人（1対1）・社内グループ・名前未取得の生IDが並び、**22店舗の単位になっていなかった**（＝全店舗が揃わない）。
- **原因**: リストを `room_settings` / `room_overview`（Botが入っている全ルーム）から生成しており、店舗マスター基準でなかった。さらに媒体（`line_message_media`）に店舗の紐づけが無かった。
- **対策**: `line_message_media` に `store_partition_key` を追加し、保存時に Webhook の店舗キー（`registry.store_partition_key`）を記録。既存分は room→store（`room_summary_settings.receipt_report_store_partition_key`）からバックフィル。admin-api `/media` を `store_partition_key` で絞り込み＋店舗単位の使用量集計（`fetchStoreMediaUsage`）に対応。`media.html` の絞り込みを **22店舗ドロップダウン**（`STORE_NAMES`）に変更し、`store_partition_key` で問い合わせ。資料側のルーム選択は従来どおり。1ルーム20MB上限（FIFO）は保存ロジックのまま。
- **反映**: コミット `0b8606c` / migration `20260531190000_media_store_partition_key.sql` / `admin-api`・`line-webhook` 再デプロイ / GitHub Pages push。
- **検証**: ドロップダウンが「全店舗＋22店舗」（計23件）になることを確認。実データの店舗別表示は運用確認。

### 2026-05-31 メディア閲覧のルーム絞り込みに個人（1対1）が混入 → 店舗ルームのみに
- **対象店舗 / 範囲**: メディア閲覧（資料フィルタ・アップロード先も同様）。
- **症状**: ルーム選択リストに `pingus [U...]` / Kazuma / yoshito 等の個人（1対1トーク）や名前無しの生IDが大量に並び、店舗ルームが選びづらい。
- **原因**: `collectRoomFilterOptions` が `room_settings` / `room_overview` の全ルーム（個人 = `U...` 含む）を選択肢に入れていた。
- **対策**: `room_id` が `C...` / `R...`（グループ/複数人トーク = 店舗ルーム）のもののみ選択肢に残し、`U...`（1対1の個人）を除外（`media.html`）。
- **反映**: コミット `6fa4bfd` / GitHub Pages push。
- **検証**: サンプルで C/R のみ残り U が除外されることを確認。

### 2026-05-31 メディア保存 ON/OFF をルーム権限（その他権限）に追加
- **対象店舗 / 範囲**: 全店舗（ルーム単位）。
- **内容**: ルームごとに「メディアを保存する/しない」を管理画面（店舗設定モーダル＞**その他権限**タブ）で切り替えられるようにした。OFF のルームは受信メディア（画像/動画/音声/ファイル）を「メディア閲覧」に保存しない（既定: **保存する**）。保存時の1ルーム20MB上限・古い順自動削除は従来どおり。
- **配線**: `room_summary_settings.media_save_enabled`（既定 true）。`line_search_bot.ts`（フラグ読込: 型/SELECT/既定/行マップ/集約）→ `line-webhook`（保存ブロックを `allowMediaSave` でゲート）、`admin-api`（型/検証/upsert/正規化payload、GET は `select("*")` で自動取得）、`index.html`（その他権限トグル＋往復JS: dom ref・dataset・load・save・override の各所）。**既存 `non_receipt_image_reply_enabled` と同一配線**にし、「保存→再表示で戻る」不具合が出ないようにした。
- **反映**: コミット `e9e6e03` / migration `20260531180000_room_media_save_enabled.sql` / `admin-api`・`line-webhook` 再デプロイ / GitHub Pages push。
- **検証**: deno check で新規型エラーなし、index.html インラインJS構文OK。実機確認は運用（その他権限で OFF→送信→メディア閲覧に出ない／ON→出る）。

### 2026-05-31 ルーム受信メディアを「メディア閲覧」へ保存（1ルーム20MB上限・古い順自動削除）
- **対象店舗 / 範囲**: 全店舗（Webhook 共通）。画像/動画/音声/ファイル。
- **症状**: メディア閲覧ページに、最近ルームから届いたメディアが出てこない（保存されていない）。
- **原因**: hocbn 移行後の Webhook は受信メディアを「会話検索用メタデータ登録」と「画像のレシート解析」しか行っておらず、メディア閲覧用テーブル `line_message_media` ＋ストレージへ**本体を保存する経路が無かった**（旧実装から引き継がれていない）。admin-api は `line_message_media` を読取/削除のみで、書込コードはリポジトリに存在しなかった。
- **対策**: `_shared/line_media_store.ts` を新設。受信メディアを取得 → `line-media`(private) バケットへアップロード → `line_message_media` へ登録（`line_message_id` で冪等）。保存後に**1ルーム合計 20MB（`ROOM_MEDIA_CAP_BYTES`）を超えた分を `created_at` 昇順でストレージ＋行ごと自動削除（FIFO）**。line-webhook のイベントループへ非ブロッキングで組込み（応答に `media_saved` 追加）。画像は「メディア閲覧への保存」と「レシート解析」の両方が独立して走る。
- **反映**: コミット `4c25ac3` / migration `20260531170000_room_media_library_bucket.sql`（line-media バケット作成・`line_message_media.message_id` を NULL 許容化・`room_id,created_at` 索引）/ `line-webhook` 再デプロイ。
- **検証**: deno check で新規型エラーなし、ヘルス HTTP 405。実機保存は運用確認（ルームへ送信→メディア閲覧で表示／20MB 超で古い順に消える）。デプロイ前の未保存分は遡及不可。

### 2026-05-31 LINE 経由アクセスを売上分析のみに制限（UI 非表示＋URL レベル強制リダイレクト）
- **対象店舗 / 範囲**: 全店舗。LINE（`?from=line` ／ LINE アプリ内ブラウザ）から来た店舗アカウント。
- **症状**: UI 刷新で各ページにサイドバーを追加した結果、LINE 経由の店舗でも全ナビが表示され、他ページ（管理コンソール等）へ遷移できてしまっていた（本来は売上分析のみ・自店舗のみ）。
- **対策**:
  1. `analytics.html`: 描画前に `data-entry=line` を立て、CSS で売上分析以外のナビ・グループ見出し・ヘッダー外部リンクを非表示（チラつき防止）。店舗セレクタは1店舗に固定（既存）。
  2. 他ページ（`index.html` / `media.html` / `reservation.html` / `message-search.html`）: `<head>` 早期に LINE 判定し、該当時は売上分析へ `location.replace` で強制リダイレクト。店舗キーは analytics で保持した `line_locked_store`（session/localStorage）から引き継ぐ。通常ブラウザは影響なし。
- **反映**: コミット `c482a73`（UI 非表示）/ `a5d64a4`（URL レベルリダイレクト＋store_key 保持）/ GitHub Pages push。
- **検証**: `?from=line` → 売上分析へリダイレクト＋店舗復元、`from=line` 無し → リダイレクトされない、を確認。フロントのみ（関数再デプロイ不要）。
- **補足**: フロント制御。サーバ側で店舗データのアクセス権限まで厳密化する場合は別途対応。

### 2026-05-31 予約表に店舗振り分け表示＋既存予約を「ビストロ サヴァサヴァ」へ紐づけ
- **対象店舗 / 範囲**: 予約表（全店舗）。予約は1つの Gmail に集約され AI が店舗を抽出。現状の実データは全てビストロ サヴァサヴァ。
- **背景/症状**: 予約表に店舗選択（22店舗）と店舗別フィルタを付けたかったが、予約データに店舗が出ていなかった。AI は `storeName` を抽出していたが、カレンダー保存用 `reservation_detail` に含めておらず、admin-api も返していなかった。
- **対策**:
  1. admin-api `/reservations/calendar` が各予約に `store_name` を返すよう最小変更。
  2. `reservation.html` に「店舗選択」ドロップダウン（全店舗＋22店舗＋未分類）と店舗別フィルタ、統計チップ、ソース色分け（食べログ=アンバー/一休=緑）、本日マーカーを追加。`resolveStoreKey` で正規化マッチング。
  3. 既存予約を「ビストロ サヴァサヴァ」へ一括バックフィル（migration）。
  4. `gmail-alert-cron` が今後の予約の `reservation_detail` に `storeName` を保存するよう修正。
  5. 店舗名マッチングを強化（末尾の敬称 様/御中 除去・大小文字無視）。食べログ「BISTRO CAVA CAVA」/一休「ビストロ サヴァサヴァ」の両表記を `bistrocavacava` に解決。
- **反映**: コミット `bbbd907`（API＋予約表）/ `72c0acc`（cron storeName＋バックフィル＋チップ簡素化）/ `94f12a3`（マッチング強化）/ migration `20260531160000_backfill_reservation_store_bistrocavacava.sql` / `admin-api`・`gmail-alert-cron` 再デプロイ / GitHub Pages push。
- **検証**: サンプルで各店舗キー解決・絞り込み・統計再計算・本日マーカーを確認。実データ確認は運用（予約表で店舗選択）。
- **未対応（要望次第）**: 店舗別「店休日（売上分析由来）」の表示、サーバ側の店舗データアクセス権限の厳密化。

### 2026-05-31 管理コンソール各ページのデザイン刷新（共有シェルへ統一）
- **対象店舗 / 範囲**: 管理画面 UI 全般（運用ロジックに影響なし。見た目とナビの統一）。
- **内容**: デザインハンドオフに沿って、接続設定／Webhook設定／利用状況／ユーザー権限（`index.html`）、メディア閲覧（`media.html`）、予約表（`reservation.html`）を**共有シェル（左サイドバー＋トップバー、ダーク基調・青アクセント）**に統一。
  - メディア閲覧・予約表をスタンドアロン旧 UI からシェルへ移行（冗長な接続フォームを撤去し、接続設定ページの共有セッションを利用。JS と DOM ID は温存）。
  - Webhook カードのリスト化＋通信中/未接続のグループ分け、利用状況の容量内訳ランキング、ユーザー権限の「利用可」マスタースイッチ＋3列機能グリッド／コンパクトな名前一覧、メディア閲覧の空状態整理。
- **反映**: コミット `dfc2b41`（index＋media）/ `bbbd907`（reservation）/ GitHub Pages push。フロントのみ（関数再デプロイ不要）。
- **検証**: 各ページをローカル/プレビューでダーク・ライト両テーマ表示確認。インライン JS 構文チェック エラー0。

### 2026-05-31 店舗別レシート解析プロンプト（管理画面で編集）＋バルぺロタ初期設定
- **対象店舗 / 範囲**: 全店舗（任意設定）。今回はバルぺロタ（精算/レジ締め形式）に初期プロンプトを投入。
- **背景**: バルぺロタは他店と違う「精算（レジ締め/日計）」形式で、項目名（合計/純売上/総取引数/客数 等）が異なり、汎用プロンプト＋軽量ビジョンモデルでは項目の取り違えが起きていた（総売上に小計や支払内訳を拾う等）。
- **対応**: 店舗ごとに「補足プロンプト」を管理画面（店舗設定モーダル＞レシート関連）で**閲覧・編集・保存**できるようにした。解析時は**既定プロンプト＋店舗の補足を連結**。未設定店は既定のまま＝**他店ゼロ影響**。基盤プロンプトは現状と一字一句同一。
  - DB: `store_receipt_analysis_prompts`。admin-api: `GET/PUT /receipts/analysis-prompt`。line-webhook: 解析時に取得し連結。
  - バルぺロタ初期プロンプト: 「精算/レジ締め。合計=総売上、総取引数=会計組数、客数=客数、純売上=純売上、消費税=内税+外税。小計/販売点数/支払内訳/お預り・お釣りは総売上に使わない」。
- **反映**: コミット `4ece07c` / migration `20260531150000_store_receipt_analysis_prompts.sql` / admin-api・line-webhook 再デプロイ / index.html push。
- **検証**: 新エンドポイント疎通（HTTP 401＝認証要求の正常応答）。バルぺロタ行を投入済み。実際の解析はレシート再送で確認（運用）。

### 2026-05-31 幽霊行クリーンアップが大文字店舗（marugoD / marugoS）を取りこぼし → 全店舗で再是正
- **対象店舗 / 範囲**: 全店舗の最終確認。特に大文字を含む marugoS（マルゴエス）・marugoD（マルゴ D）。
- **症状**: 幽霊行の一括クリーンアップ後、全店舗を再検証したところ marugoS にオーファンが1件残っていた。
- **原因**: クリーンアップ migration のテーブル存在チェックに `to_regclass('public.' || t)` を使っており、識別子が小文字化されるため大文字混在テーブル（`line_receipt__marugoD` / `line_receipt__marugoS`）を「存在しない」と誤判定してスキップしていた（DELETE文の `%I` は正しいが、存在チェックだけ取りこぼし）。
- **対策**: `to_regclass(format('public.%I', t))` で正しく引用して再クリーンアップ（v2）。各テーブルを個別の例外ハンドラで処理し、1テーブルの失敗で他を止めないようにした。なお**実行時コードは receipt_table の文字列完全一致で削除する**ため大文字小文字の影響を受けず、再発防止は全店舗で有効（コード変更・再デプロイは不要）。
- **反映**: migration `20260531133000_cleanup_receipt_search_orphans_v2.sql`。
- **検証**: 全店舗の検索インデックスを再走査し、**オーファン 0件**（receipt_row_id型・null型とも）を確認。本体削除経路は store_receipt.ts の3関数のみで、管理画面・cron・シート同期は本体を削除しないことも確認済み（抜け道なし）。

### 2026-05-31 売上検索に「幽霊行」が出て削除できない（バルぺロタ で発覚）
- **対象店舗 / 範囲**: バルぺロタ (barpelota) で発覚。仕組みは共通のため**全店舗**に影響。
- **症状**: 1日分を売上検索すると同一日に複数件（例: 5件）表示され、値もバラバラ（0組0人の行が混在）。「削除」を押すと「レシートはありません（該当のレシート解析が見つかりませんでした）」となり消せない。実際は本体に1件しか無かった。
- **原因**: 売上検索は本体テーブルではなく検索インデックス `line_room_receipt_search` を参照する。本体レシートの削除経路（①削除ボタン by receipt_row_id ②削除ボタン by メッセージID ③重複「置き換え」の同日削除）が**いずれもインデックスを消していなかった**ため、削除・置き換えのたびに本体に無い「幽霊行（オーファン）」がインデックスに残り、検索に出続けていた。削除ボタンは本体を `receipt_row_id` で消しに行くが、本体に無いため失敗していた。
- **対策**:
  1. 3つの削除経路すべてで `line_room_receipt_search` を同期削除（`store_receipt.ts`）。
  2. 本体が無い幽霊行は、インデックス側を消して検索から除去し**成功扱い**に（ボタンで掃除できるように）。
  3. 既存オーファンを migration で一括削除（本体に紐づかないインデックス行のみ。実レシートは無傷）。
- **反映**: コミット `f276895` / migration `20260531120000_cleanup_receipt_search_orphans.sql` / `line-webhook` 再デプロイ。
- **検証**: barpelota 2026-05-30 = インデックス 5件 → **1件**（本物 receipt_row_id=3559 のみ）。本体は1件のまま。全店舗のオーファンも同 migration で除去。

### 2026-05-31 Webhook 再送で同じ返信が何度も来る（全店舗）
- **対象店舗 / 範囲**: 全店舗（Webhook 共通）。検索・レシート・各種ボタン postback すべて。
- **症状**: 1回送信しただけで「売上検索結果」等が何通も返ってくる。
- **原因**: LINE は応答が遅い/失敗すると同一 `webhook_event_id` で再送する。`insertRawWebhookEvent` が重複エラー(23505)を成功扱いで握りつぶし、再送イベントを再処理→再返信していた（べき等性が無かった）。加えて、成功した検索が応答の成功判定に含まれず 500 を返し、LINE 再送を誘発していた。
- **対策**: 23505 を「再送（既処理）」として検知し、そのイベントを丸ごとスキップ（`webhook_event_id` ユニーク索引で dedup）。応答の成功判定に検索成功(`searchGuideHandled`)を追加し不要な 500 を解消（`line-webhook/index.ts`）。
- **反映**: コミット `181f60f` / `line-webhook` 再デプロイ。
- **検証**: ヘルス HTTP 405（正常起動）。再送が来てもスキップされ重複返信しない。

### 2026-05-31 天候を恒久保存（再取得で消えない）
- **対象店舗 / 範囲**: 全店舗（売上分析の天候表示）。
- **症状**: 取得した天気が保存されず（本番のキャッシュ0行）、毎回取得し直していた。
- **原因**: 旧実装は予算テーブル `line_sales_month_budgets` を `weather:*` キーで流用していたが実際には保存されていなかった。
- **対策**: 専用テーブル `line_weather_daily`（cache_key × 日付）を新設し、`weather_daily.ts` の保存/読込を専用テーブル方式に変更。一度取得した過去天気は恒久保存。
- **反映**: コミット `a5f6e98` / migration `20260530170000_weather_daily_table.sql` / `admin-api` 再デプロイ。
- **検証**: 同一日を再取得しても upsert で更新のみ（重複・消失なし）を実地確認。

### 2026-05-30 レシート修正中に別日を検索すると修正画面に吸われる
- **対象店舗 / 範囲**: 全店舗。
- **症状**: 「この結果を修正」後に新しい日付を送ると、売上検索結果カードが出ず、いきなりレシート修正カードが再表示される。
- **原因**: 修正セッション（項目番号待ち = select_field）が残っていると、日付入力が修正フローに吸われていた。
- **対策**: select_field 中に日付検索（YYYYMMDD / YYYYMM）が来たら修正を終了し、検索（売上検索結果）へフォールスルー（`receipt_correction.ts`）。日付項目の値入力には影響しない。
- **反映**: コミット `941705e` / `line-webhook` 再デプロイ。

### 2026-05-30 売上検索結果カードの「検索メニュー」→「キャンセル」
- **対象店舗 / 範囲**: 全店舗。
- **内容**: 「検索メニュー」ボタンを削除し「キャンセル」を追加。押すと検索フローを終了（`clearSearchPending`）＋「検索をキャンセルしました。」と返信。
- **反映**: コミット `6f4f5f4` / `line-webhook` 再デプロイ。

### 2026-05-30 ルーム権限の保存が再表示で元に戻る
- **対象店舗 / 範囲**: 全店舗（管理画面のルーム権限）。
- **症状**: 「レシート修正の返信を許可」「画像解析結果を送信（非レシート）」を変更・保存しても、再オープンで戻る。
- **原因**: この2項目 (`receipt_correction_reply_enabled` / `non_receipt_image_reply_enabled`) が保存時の PUT ペイロードに含まれておらず、admin-api の upsert で毎回既定値へリセットされていた。
- **対策**: 行データ取得/反映・レンダリング・保存ペイロード・新規ルーム追加の各所に2項目を追加し、UI⇄DB の往復を成立させた（`index.html`）。
- **反映**: コミット `54d719a` / GitHub Pages へ push（フロントのみ。admin-api/DBは対応済み）。

### 2026-05-30 国民の祝日を内閣府CSVから取得
- **対象店舗 / 範囲**: 全店舗（予算配分の祝日判定・分析画面の祝日表示）。
- **内容**: ハードコード表（2022–2028）に代えて、内閣府 `syukujitsu.csv` をサーバ取得（Shift_JIS デコード・12h キャッシュ・失敗時ハードコードへフォールバック）。予算配分（ダッシュボード集計・LINE返信・シート同期）と分析画面の祝日判定で使用。
- **反映**: コミット `873d66a` / `admin-api`・`line-webhook`・`receipt-sheets-sync-cron` 再デプロイ。

### 2026-05-30 BISTRO サヴァサヴァ 5月総売上が誤り（手入力オーバーライド）
- **対象店舗 / 範囲**: ビストロ サヴァサヴァ (bistrocavacava)。
- **症状**: 5月の総売上が 927,900 と表示（正しくはレシート合算 1,097,300）。
- **原因**: `line_sales_manual_month_gross` に古い手入力の月総額(927,900)が残っており、自動集計を上書きしていた。
- **対策**: 当該手入力行をクリア（削除）。以後はレシート合算(1,097,300)が表示される。
- **反映**: 対象1行の削除（DB）。
