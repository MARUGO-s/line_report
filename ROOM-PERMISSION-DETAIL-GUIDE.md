# ルーム権限・Webhook 設定 — 細かい仕様ガイド（運用・開発向け）

> **先に読む:** [ルーム連携ガイド](./ROOM-LINKING-GUIDE.md)（自動連携・セキュリティ）  
> **Gmail 予約通知:** [RESERVATION-GMAIL-GUIDE.md](./RESERVATION-GMAIL-GUIDE.md)（LINE への過去5件表示・DB）  
> **動作確認:** [ROOM-PERMISSION-TEST-CHECKLIST.md](./ROOM-PERMISSION-TEST-CHECKLIST.md)

管理画面 `index.html` の **権限の意味**・**画面の出し分け**・**保存の届き方**をまとめたドキュメントです。

---

## 1. 用語

| 用語 | 意味 |
|------|------|
| **連携ルーム** | `room_summary_settings` に登録され、当該店舗 Webhook の `receipt_report_store_partition_key` が付いた LINE トーク |
| **店舗一括** | その Webhook の **全連携ルーム** に同じフラグを保存するときの設定 |
| **ルーム個別** | 連携ルームのうち **1 トークだけ** 店舗一括と違う設定にするとき（優先順位「ルーム個別」） |
| **全体権限** | 全店舗・全ルームへの一括（順位「全体」。ルーム個別は上書きしない） |
| **permission モード** | 店舗カードの「権限・機能設定」（`data-webhook-mode="permission"`） |
| **setup モード** | 初回セットアップ用カード（`data-webhook-mode="setup"`。カレンダー個別 UI なし） |

---

## 2. 設定の優先順位（上書きルール）

```
全体（0）  <  店舗 Webhook（1）  <  ルーム個別（2）
```

| 操作 | 届く範囲 | ルーム個別（順位2）のルーム |
|------|----------|---------------------------|
| **全体権限を編集** → 保存 | 全店舗・全連携ルーム | **スキップ**（上書きされない） |
| 店舗 **権限・機能設定** → 保存 | その店舗の全連携ルーム | **スキップ** |
| 連携ルームの **個別設定** → 保存 | **そのルームだけ** | 順位が **ルーム個別** に固定 |

- **継承** ボタン: そのルームの DB 上書きをやめ、店舗＋全体のマージ値に戻す（順位は店舗相当に戻るイメージ）
- 店舗の既定は `localStorage` の `line_webhook_default_config__{storeKey}` にも保存される（連携前の受信済みルームのデフォルト）

---

## 3. モーダル 3 タブと「保存 1 回」の意味

画面上は **レシート関連 / カレンダー・予約 / その他権限** の 3 タブに分かれていますが、**保存ボタン 1 回**で次のように処理されます。

| タブ | 保存時の届き方（permission・店舗保存） |
|------|--------------------------------------|
| **レシート関連** | 中間/月末 ON/OFF・メディア・解析返信 → **全連携ルーム同一** |
| **レシート関連** | 送信 **日刻**（毎月○日 ○:○○）→ **プルダウンで選んでいるルームだけ**（`localStorage`） |
| **カレンダー/予約** | 上部 **店舗一括** → **全連携ルーム** |
| **カレンダー/予約** | **ルーム個別 ON** のときだけ → **選択中ルームだけ** 上書き |
| **その他権限** | AI 返信・会話検索・要約 → **全連携ルーム同一** |

`is_enabled`（ルーム有効）は保存時に **複数タブのフラグの OR** で再計算されます。  
**会話検索だけ ON** では `is_enabled` は立ちません（bot 返信・要約・メディア・カレンダー系などが必要）。

---

## 4. カレンダー／予約タブ — なぜ二段に見えるか

### 4.1 連携ルーム数による表示（重要）

| 連携ルーム数 | 画面 |
|--------------|------|
| **0 件** | 「連携してから設定」と案内。チェックは出ない |
| **1 件** | **店舗一括のチェックだけ**（ルーム名付き）。「1 件だけなのでルーム差は不要」と説明。**下の「ルーム個別」ブロックは非表示** |
| **2 件以上** | **上: 店舗一括**（全ルームに同じ設定）／**下: ルーム個別**（対象ルームごとに差を付けられる） |

→ **複数ルームがある店舗だけ**「一括と個別の違い」が意味を持ちます。

### 4.2 2 件以上のときの見え方（ダブり防止）

| 状態 | 表示 |
|------|------|
| **個別設定 OFF** | **上だけ** 店舗一括の 5 チェック。下は説明＋「このルームだけ一括と違う設定にする」トグルのみ |
| **個別設定 ON** | **上の店舗一括は隠す**。下の個別パネルだけ（明日予定・GCal・Gmail など） |

個別 ON のとき保存は **Perm 側の値だけ**（インライン一括はマージしない）。

### 4.3 対象ルームの選び方

- **レシート関連**タブの **「対象ルーム」** プルダウンと **同じルーム** を参照
- カレンダー個別で「いま編集するルーム: …」と表示されるのもこの選択

### 4.4 カレンダー項目の依存（タブ内）

| 操作 | 効果 |
|------|------|
| GCal 自動登録 OFF | 「登録内容を LINE で返信」「低確度確認」が **操作不可** |
| 明日予定 ON ＋ 個別 ON | 配信時刻・予定がある日のみ・最大件数が表示 |

---

## 5. ルーム表示名（店舗名と LINE トーク名）

LINE の **グループ名**（例: `Cava Cava`）が `room_name` に入っていることがあり、**店舗名と混同**しやすいです。

### 5.1 正式な店舗表記（優先ラベル）

| store_partition_key | 管理画面で優先する表記 |
|---------------------|----------------------|
| `bistrocavacava` | **Bistro CAVACAVA** |

定義: `pages-config.js` の `STORE_PREFERRED_UI_LABELS`  
関数: `getPreferredStoreDisplayLabel`, `resolveRoomDisplayLabelForWebhook`（`index.html`）

### 5.2 いつ置き換えるか

Webhook 設定モーダル内（対象ルームプルダウン・「いま編集するルーム」など）で、

- 当該店舗が `bistrocavacava` など登録済み店舗で、かつ
- LINE 名が `Cava Cava` など **紛らわしい別名** と判定されたとき

→ **Bistro CAVACAVA** を表示（LINE 名は店舗名と明らかに違うときだけ `（LINE: …）` を付与）。

### 5.3 店舗カードのタイトル

Webhook カード見出しは API / `STORE_NAMES` の **日本語店名**（例: ビストロ サヴァサヴァ）のまま。  
**モーダル内のルーム選択**だけ英表記優先ラベルを使います。

---

## 6. レシート関連タブ — 補足

| 項目 | 内容 |
|------|------|
| **対象ルーム** | 連携済みルームのプルダウン。未連携は「受信済みルームを連携」 |
| **送信時刻の個別** | 「このルームで送信時刻を個別設定」ON 時のみ日・時刻入力。OFF は店舗一括時刻 |
| **中間/月末チェック** | 保存で **全連携ルーム** に同じ ON/OFF |

---

## 7. 自動連携（権限の前提）

詳細は [ROOM-LINKING-GUIDE.md](./ROOM-LINKING-GUIDE.md)。

- **既定 ON** — Webhook 受信・管理画面同期で **承認なし** 連携
- 連携時に店舗既定の権限（中間レポート ON など）が付く
- 無効化: `RECEIPT_ROOM_AUTO_LINK=0`

---

## 8. 画面の色（状態の見方）

カード全面をオレンジ／緑に塗らず、**左のアクセント線**と小さなバッジで状態を示します。

| 表示 | 意味 |
|------|------|
| 左線・琥珀系 | 未連携の受信ルームが残っている |
| 左線・緑系 | 連携済み・未連携 0 |
| 赤枠の注意（Webhook 設定上） | 自動連携（承認なし）の説明 |

ライトモードの **店名（カードタイトル）** は `#111111`（ほぼ黒）で背景と区別。

---

## 9. コード参照（主要）

| 内容 | 場所 |
|------|------|
| 優先順位・全体/店舗/個別保存 | `CONFIG_PRIORITY`, `saveGlobalPermissionFromModal`, `saveRoomConfigModal` |
| カレンダー表示切替 | `syncRoomConfigCalendarTabLayout`, `getLinkedRoomCountInModal` |
| カレンダー保存マージ | `buildRoomConfigDraftFromModal`, `resolveCalendarGmailSettingsForRoom` |
| ルーム表示名 | `resolveRoomDisplayLabelForWebhook`, `pages-config.js` |
| 自動連携 | `supabase/functions/_shared/auto_link_room.ts` |
| 連携検出 | `admin_receipt_sales.ts` … `detected_room_ids` |

---

## 10. よくある質問

**Q. 「Cava Cava」と出るが店舗は Bistro CAVACAVA では？**  
A. LINE グループ名です。モーダルでは **Bistro CAVACAVA** に置き換えて表示します（§5）。

**Q. カレンダーで同じチェックが二重に見える**  
A. 連携 **2 件以上** で個別 ON のときは上（一括）を隠します。1 件だけの店舗では個別ブロック自体を出しません（§4）。

**Q. 個別設定の意味がわからない**  
A. **同じ店舗 Webhook に LINE グループが 2 つ以上**あるときだけ、トーク A だけ明日予定 ON・トーク B は OFF、のように差を付けられます。

**Q. 保存したのにルームによって違う**  
A. そのルームが **順位「ルーム個別」** のまま、またはレシート **送信時刻だけ** ルーム別 `localStorage` です（§2・§3）。

---

*最終更新: 2026-05 — カレンダー出し分け・Bistro CAVACAVA 表示・配色調整を反映*
